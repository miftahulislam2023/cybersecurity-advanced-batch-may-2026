# লিনাক্স ইউজার এবং গ্রুপ ম্যানেজমেন্ট (Linux User & Group Management)

এই নোটটি লিনাক্স অপারেটিং সিস্টেমে ইউজার (User) এবং গ্রুপ (Group) পরিচালনার বিস্তারিত ধারণা দেওয়ার জন্য তৈরি করা হয়েছে। এটি অনুসরণ করে শিক্ষার্থীরা খুব সহজেই সিস্টেম অ্যাডমিনিস্ট্রেশনের এই গুরুত্বপূর্ণ বিষয়টি আয়ত্ত করতে পারবে।

---

## ১ ভূমিকা (Introduction)

লিনাক্স ইনস্টল করার সময় সাধারণত একটি সাধারণ ইউজার অ্যাকাউন্ট তৈরি হয় এবং সেই ইউজারকে `sudo` (অ্যাডমিনিস্ট্রেটিভ পারমিশন) ব্যবহারের ক্ষমতা দেওয়া হয় অথবা ইনস্টলেশনের সময় `root` (সুপারইউজার) এর পাসওয়ার্ড সেট করতে বলা হয়। লিনাক্সে একটি সাধারণ (non-root) ইউজার লগইন করে সিস্টেম চালাতে পারে এবং প্রয়োজনে `sudo` ব্যবহার করে অ্যাডমিনিস্ট্রেটিভ কাজ করতে পারে।

যদি একটি কম্পিউটার শুধুমাত্র একজন ব্যবহার করেন, তবে একটি অ্যাকাউন্টই যথেষ্ট। কিন্তু যদি একাধিক ব্যক্তি একই কম্পিউটার ব্যবহার করেন, তবে প্রত্যেকের জন্য আলাদা অ্যাকাউন্ট থাকা জরুরি। আলাদা অ্যাকাউন্ট থাকার সুবিধাগুলো হলো:

* **নিরাপত্তা এবং প্রাইভেসি:** ফাইলের অ্যাক্সেস নিয়ন্ত্রণ করা যায়। যেমন, একজনের হোম ডিরেক্টরিতে (Home Directory) অন্য ইউজার প্রবেশ করতে পারে না।
* **অ্যাডমিন ক্ষমতা নিয়ন্ত্রণ:** `sudo` কমান্ডের মাধ্যমে নির্দিষ্ট ইউজারকে নির্দিষ্ট অ্যাডমিন কাজ করার ক্ষমতা দেওয়া যায়। এতে করে কে কখন কী কমান্ড চালিয়েছে তা সিস্টেম লগে সেভ থাকে।
* **গ্রুপ ম্যানেজমেন্ট:** প্রতিটি ইউজারের আলাদা গ্রুপ মেম্বারশিপ থাকতে পারে, যা সিস্টেম ম্যানেজমেন্টকে সহজ করে।

কিছু লিনাক্স ডিস্ট্রিবিউশনে (যেমন Red Hat) নতুন ইউজার তৈরি করলে স্বয়ংক্রিয়ভাবে একটি User Private Group (UPG) তৈরি হয়। এই ক্ষেত্রে ইউজারের নাম এবং গ্রুপের নাম একই থাকে। অন্য ডিস্ট্রিবিউশনগুলোতে ইউজার তৈরি করলে তাদের ডিফল্টভাবে `users` নামক প্রাইমারি গ্রুপে যুক্ত করা হয়।

**পরামর্শ:** ইউজার তৈরি করার আগে গ্রুপগুলো তৈরি করে নেওয়া ভালো। এতে করে ইউজার তৈরি করার সময়ই তাদের সঠিক গ্রুপে যুক্ত করা যায়।

---

## ২ গ্রুপ (Groups)

গ্রুপ তৈরি করার প্রধান উদ্দেশ্য হলো একাধিক ইউজারের মধ্যে ফাইল শেয়ার করা। ধরুন, একটি প্রোজেক্টে কয়েকজন মিলে কাজ করছে, তখন তাদের সবাইকে একটি নির্দিষ্ট গ্রুপে যুক্ত করে ফাইলের পারমিশন সেট করে দিলে তারা সহজেই একসঙ্গে কাজ করতে পারবে।

গ্রুপের তথ্য চেক করার জন্য `/etc/group` ফাইলটি দেখা যায়।

```bash
miftah@miftahcoding:~# grep root /etc/group
root:x:0:

miftah@miftahcoding:~# getent group root
root:x:0:

```

### ২.১ গ্রুপ তৈরি করা (Creating A Group)

নতুন গ্রুপ তৈরি করতে `groupadd` কমান্ড ব্যবহার করা হয়। `-g` অপশন ব্যবহার করে নির্দিষ্ট Group ID (GID) দেওয়া যায়।

```bash
miftah@miftahcoding:~# groupadd -g 1005 research
miftah@miftahcoding:~# grep research /etc/group
research:x:1005:

```

যদি `-g` না দেওয়া হয়, তবে সিস্টেম স্বয়ংক্রিয়ভাবে সর্বশেষ GID এর পরের নম্বরটি বসিয়ে দেয়।

```bash
miftah@miftahcoding:~# groupadd development
miftah@miftahcoding:~# grep development /etc/group
development:x:1006:

```

**উদাহরণ (আরও কয়েকটি):**

```bash
# নির্দিষ্ট GID ছাড়া সাধারণ গ্রুপ তৈরি
miftah@miftahcoding:~# groupadd frontend_devs

# নির্দিষ্ট GID সহ গ্রুপ তৈরি
miftah@miftahcoding:~# groupadd -g 2000 backend_devs

```

#### ২.১.১ Group ID (GID) বিষয়ক সতর্কতা

UPG (User Private Group) সাপোর্ট করে এমন ডিস্ট্রিবিউশনে ইউজার তৈরি করলে একই নম্বরের UID এবং GID তৈরি হয়। তাই নিজে থেকে GID দেওয়ার সময় এমন রেঞ্জ ব্যবহার করা উচিত নয় যা পরবর্তীতে ইউজার তৈরি করার সময় কনফ্লিক্ট করতে পারে।

সাধারণত ৫০০ বা ১০০০ এর নিচের GID গুলো সিস্টেমের জন্য সংরক্ষিত থাকে। সিস্টেম গ্রুপ তৈরি করতে `-r` ব্যবহার করা হয়।

```bash
miftah@miftahcoding:~# groupadd -r sales
miftah@miftahcoding:~# getent group sales
sales:x:999:

```

#### ২.১.২ গ্রুপের নামকরণের নিয়ম

* প্রথম অক্ষর অবশ্যই ইংরেজি ছোট হাতের অক্ষর (a-z) বা আন্ডারস্কোর (_) হতে হবে।
* সর্বোচ্চ ৩২ ক্যারেক্টার ব্যবহার করা যায়, তবে ১৬ ক্যারেক্টারের মধ্যে রাখা নিরাপদ।
* মাঝখানে ড্যাশ (-) ব্যবহার করা যাবে, তবে শেষে ড্যাশ ব্যবহার করা উচিত নয়।

### ২.২ গ্রুপ পরিবর্তন করা (Modifying a Group)

গ্রুপের নাম পরিবর্তন করতে `groupmod -n` এবং GID পরিবর্তন করতে `groupmod -g` ব্যবহার করা হয়।

**গ্রুপের নাম পরিবর্তন:** ফাইলের অ্যাক্সেসে কোনো সমস্যা হয় না, কারণ সিস্টেম গ্রুপের নাম নয় বরং GID দিয়ে পারমিশন চেক করে।

```bash
miftah@miftahcoding:~# ls -l index.html
-rw-r-----. 1 root sales 0 Aug  1 13:21 index.html

miftah@miftahcoding:~# groupmod -n clerks sales

miftah@miftahcoding:~# ls -l index.html
-rw-r-----. 1 root clerks 0 Aug  1 13:21 index.html

```

**GID পরিবর্তন:** GID পরিবর্তন করলে আগের GID তে থাকা ফাইলগুলো এতিম (Orphaned) হয়ে যায়। তখন ফাইলের ওনারশিপে গ্রুপের নামের বদলে শুধু পুরোনো নম্বরটি দেখায়।

```bash
miftah@miftahcoding:~# groupmod -g 10003 clerks
miftah@miftahcoding:~# ls -l index.html
-rw-r-----. 1 root 491 13370 Aug  1 13:21 index.html

```

*এতিম ফাইল খুঁজে বের করতে `find / -nogroup` কমান্ড ব্যবহার করা যায়।*

### ২.৩ গ্রুপ মুছে ফেলা (Deleting a Group)

গ্রুপ ডিলিট করতে `groupdel` কমান্ড ব্যবহার করা হয়। তবে কোনো ইউজারের প্রাইমারি গ্রুপ সরাসরি ডিলিট করা যায় না।

```bash
miftah@miftahcoding:~# groupdel clerks

```

*সতর্কতা: গ্রুপ ডিলিট করলে ঐ গ্রুপের অধীনে থাকা ফাইলগুলো Orphaned হয়ে যাবে।*

---

## ৩ ইউজার (Users)

ইউজারের সাধারণ তথ্য `/etc/passwd` ফাইলে এবং পাসওয়ার্ড সংক্রান্ত সংবেদনশীল তথ্য `/etc/shadow` ফাইলে সংরক্ষিত থাকে। ম্যানুয়ালি এই ফাইলগুলো এডিট না করে কমান্ড ব্যবহার করা বেশি নিরাপদ।

### ৩.১ ইউজার কনফিগারেশন ফাইল (`/etc/default/useradd`)

`useradd` কমান্ড চালানোর সময় কোন ডিফল্ট ভ্যালুগুলো কাজ করবে, তা জানতে `useradd -D` ব্যবহার করা হয়।

```bash
miftah@miftahcoding:~# useradd -D 
GROUP=100
HOME=/home
INACTIVE=-1
EXPIRE=
SHELL=/bin/bash
SKEL=/etc/skel
CREATE_MAIL_SPOOL=yes

```

| সেটিংস                   | বর্ণনা                                                      | কাস্টমাইজ করার অপশন |
| --------------------- | --------------------------------------------------------- | --------------- |
| **GROUP=100**         | ইউজারের ডিফল্ট প্রাইমারি গ্রুপ (সাধারণত users)।                       | `-g`            |
| **HOME=/home**        | যেখানে ইউজারের হোম ডিরেক্টরি তৈরি হবে।                                 | `-b`            |
| **INACTIVE=-1**       | পাসওয়ার্ড মেয়াদোত্তীর্ণ হওয়ার কতদিন পর অ্যাকাউন্ট ডিজেবল হবে (-1 মানে কখনো না)। | `-f`            |
| **EXPIRE=**           | অ্যাকাউন্টের নির্দিষ্ট মেয়াদ শেষ হওয়ার তারিখ।                            | `-e`            |
| **SHELL=/bin/bash**   | লগইন করার পর ডিফল্ট শেল।                                      | `-s`            |
| **SKEL=/etc/skel**    | এই ফোল্ডারের ভেতরের সব ফাইল নতুন ইউজারের হোম ডিরেক্টরিতে কপি হয়ে যায়।        | `-k`            |
| **CREATE_MAIL_SPOOL** | ইউজারের লোকাল মেইল রিসিভ করার জন্য ফাইল তৈরি হবে কি না।                  |                 |

ভ্যালু পরিবর্তন করার উদাহরণ (INACTIVE ভ্যালু ৩০ দিন করা):

```bash
miftah@miftahcoding:~# useradd -D -f 30

```

### ৩.২ ইউজার কনফিগারেশন ফাইল (`/etc/login.defs`)

এই ফাইলটিতে ইউজারের পাসওয়ার্ড এবং আইডির লিমিটেশন সংক্রান্ত ডিফল্ট কনফিগারেশন থাকে।

```bash
miftah@miftahcoding:~# grep -Ev '^#|^$' /etc/login.defs

```

**গুরুত্বপূর্ণ কিছু ভ্যালু:**

* `PASS_MAX_DAYS 99999`: একটি পাসওয়ার্ড সর্বোচ্চ কতদিন ব্যবহার করা যাবে।
* `PASS_MIN_DAYS 0`: পাসওয়ার্ড পরিবর্তন করার পর অন্তত কতদিন তা ব্যবহার করতে হবে।
* `PASS_WARN_AGE 7`: পাসওয়ার্ড মেয়াদোত্তীর্ণ হওয়ার কতদিন আগে থেকে ইউজারকে ওয়ার্নিং দেওয়া হবে।
* `UID_MIN 500 / 1000`: সাধারণ ইউজারের ইউজার আইডি (UID) এখান থেকে শুরু হবে।
* `UMASK 077`: নতুন হোম ডিরেক্টরি তৈরি হলে তার ডিফল্ট পারমিশন কী হবে।

### ৩.৩ ইউজার তৈরি করার পরিকল্পনা (Account Considerations)

* **Username:** গ্রুপের নামের মতো একই নিয়ম মেনে নাম দিতে হবে।
* **UID (-u):** রুট ইউজারের UID 0। সিস্টেম অ্যাকাউন্টের রেঞ্জ 1-999। সাধারণ ইউজারের জন্য 1000 থেকে শুরু করা ভালো।
* **Primary Group (-g):** ইউজারের প্রধান গ্রুপ।
* **Supplementary Group (-G):** ইউজার আরও যেসব গ্রুপের সদস্য হবে।
* **Home Directory (-m, -M, -d, -b):** `-m` দিলে ডিরেক্টরি তৈরি হবে। `-M` দিলে তৈরি হবে না। `-d` দিয়ে কাস্টম পাথ দেওয়া যায়।
* **Shell (-s):** সাধারণ ইউজারের জন্য `/bin/bash`। আর সিস্টেম ইউজারের জন্য `/sbin/nologin` (যাতে লগইন করতে না পারে)।
* **Comment (-c):** ইউজারের পুরো নাম বা পরিচয় রাখার জন্য (GECOS ফিল্ড)।

### ৩.৪ ইউজার তৈরি করা (Creating a User)

সব অপশন মিলিয়ে একটি ইউজার তৈরি করার উদাহরণ:

```bash
miftah@miftahcoding:~# useradd -u 1009 -g users -G sales,research -m -c 'Jane Doe' jane 

```

এই কমান্ডের অর্থ: `jane` নামের ইউজার তৈরি করো, যার UID হবে 1009, প্রাইমারি গ্রুপ `users`, অতিরিক্ত গ্রুপ `sales` ও `research`, হোম ডিরেক্টরি তৈরি করো (`-m`), এবং পুরো নাম 'Jane Doe'।

ফলাফল যাচাই:

```bash
miftah@miftahcoding:~# grep jane /etc/passwd
jane:x:1009:100:Jane Doe:/home/jane:/bin/sh

miftah@miftahcoding:~# grep jane /etc/group
research:x:1005:jane
sales:x:999:jane

```

### ৩.৫ পাসওয়ার্ড (Passwords)

ইউজার তৈরি করার পর তার পাসওয়ার্ড সেট না করা পর্যন্ত সেই অ্যাকাউন্ট দিয়ে লগইন করা যায় না। ভালো পাসওয়ার্ডের ক্ষেত্রে দৈর্ঘ্য বড় হওয়া, অক্ষর ও সংকেতের মিশ্রণ থাকা জরুরি।

#### ৩.৫.১ পাসওয়ার্ড সেট করা

অ্যাডমিন হিসেবে যেকোনো ইউজারের পাসওয়ার্ড সেট করতে:

```bash
miftah@miftahcoding:~# passwd jane
Enter new UNIX password: 
Retype new UNIX password: 

```

সাধারণ ইউজার নিজে পাসওয়ার্ড পরিবর্তন করতে চাইলে শুধু `passwd` কমান্ড লিখতে হবে। রুট ইউজারের ক্ষেত্রে পাসওয়ার্ডের সাধারণ নিয়মাবলি (যেমন: খুব ছোট পাসওয়ার্ড) প্রযোজ্য হয় না, তবে সাধারণ ইউজারদের কঠিন পাসওয়ার্ডই দিতে হয়।

#### ৩.৫.২ পাসওয়ার্ডের মেয়াদ পরিচালনা (Password Aging)

পাসওয়ার্ডের মেয়াদ নিয়ন্ত্রণ করতে `chage` কমান্ড ব্যবহার করা হয়।

| অপশন | বর্ণনা                                 |
| ---- | ------------------------------------ |
| `-l` | ইউজারের পাসওয়ার্ড এজিং এর বর্তমান অবস্থা দেখায়।   |
| `-M` | সর্বোচ্চ কতদিন পাসওয়ার্ডটি বৈধ থাকবে।           |
| `-m` | সর্বনিম্ন কতদিন পাসওয়ার্ডটি ব্যবহার করতে হবে।    |
| `-W` | মেয়াদ শেষ হওয়ার কতদিন আগে ওয়ার্নিং দেবে।         |
| `-E` | নির্দিষ্ট তারিখে অ্যাকাউন্ট এক্সপায়ার করে দেওয়ার জন্য। |

উদাহরণ (পাসওয়ার্ডের মেয়াদ ৬০ দিন সেট করা):

```bash
miftah@miftahcoding:~# chage -M 60 jane

```

### ৩.৬ ইউজার মডিফাই করা (Modifying a User)

বিদ্যমান ইউজার অ্যাকাউন্টে কোনো পরিবর্তন আনতে `usermod` কমান্ড ব্যবহার করা হয়।

| অপশন  | কাজ                                                         |
| ----- | ---------------------------------------------------------- |
| `-l`  | ইউজারের লগইন নেম (username) পরিবর্তন করা।                        |
| `-L`  | ইউজার অ্যাকাউন্ট লক (Lock) করা।                                  |
| `-U`  | ইউজার অ্যাকাউন্ট আনলক (Unlock) করা।                              |
| `-aG` | **খুব গুরুত্বপূর্ণ:** ইউজারের বর্তমান গ্রুপগুলো ঠিক রেখে নতুন কোনো গ্রুপে যুক্ত করা। |

**সতর্কতা:** শুধু `-G` ব্যবহার করলে ইউজার তার পুরোনো সব সাপ্লিমেন্টারি গ্রুপ থেকে রিমুভ হয়ে যাবে এবং শুধু নতুন গ্রুপে যুক্ত হবে। তাই সবসময় `-aG` ব্যবহার করা উচিত।

উদাহরণ (jane কে development গ্রুপে যুক্ত করা):

```bash
miftah@miftahcoding:~# usermod -aG development jane

```

ইউজার লক করার উদাহরণ:

```bash
miftah@miftahcoding:~# usermod -L jane

```

### ৩.৭ ইউজার মুছে ফেলা (Deleting a User)

ইউজার ডিলিট করার জন্য `userdel` ব্যবহার করা হয়।

শুধুমাত্র ইউজার অ্যাকাউন্ট ডিলিট করতে (হোম ডিরেক্টরি থেকে যাবে):

```bash
miftah@miftahcoding:~# userdel jane

```

ইউজারের অ্যাকাউন্ট এবং তার হোম ডিরেক্টরি (ফাইলসহ) সব মুছে ফেলতে `-r` অপশন ব্যবহার করতে হবে:

```bash
miftah@miftahcoding:~# userdel -r jane

```

*সতর্কতা: `-r` দিলে ইউজারের যাবতীয় ফাইল ডিলিট হয়ে যাবে, যা ব্যাকআপ না থাকলে আর ফিরে পাওয়া সম্ভব নয়।*