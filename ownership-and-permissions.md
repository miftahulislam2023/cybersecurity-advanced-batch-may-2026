# লিনাক্স ফাইল পারমিশন, ওনারশিপ এবং umask এর পূর্ণাঙ্গ গাইড

এই ডকুমেন্টে লিনাক্সের ফাইল ওনারশিপ (Ownership), গ্রুপ ম্যানেজমেন্ট, পারমিশন (Permissions), পারমিশন অ্যানালাইসিস সিনারিও, পারমিশন পরিবর্তন করার নিয়ম এবং ডিফল্ট umask কনফিগারেশন সম্পর্কে বিস্তারিত আলোচনা করা হয়েছে। শিক্ষার্থীদের সহজে বোঝার জন্য এখানে বাস্তব উদাহরণ এবং ব্যাখ্যা যুক্ত করা হয়েছে।

---

## ১. ফাইল ওনারশিপ (File Ownership) এর ধারণা

সাধারণত, যে ইউজার (User) কোনো ফাইল তৈরি করেন, তিনি সেই ফাইলের মালিক বা 'ওনার' হন। এই ওনারশিপ পরিবর্তন করা সম্ভব, তবে এর জন্য অ্যাডমিনিস্ট্রেটিভ (root) প্রিভিলেজ প্রয়োজন।

যদিও টার্মিনালে কমান্ডগুলো ফাইলের মালিকের নাম টেক্সট হিসেবে দেখায়, কিন্তু অপারেটিং সিস্টেম ভেতরে ভেতরে ইউজারদের একটি নম্বর বা **User ID (UID)** দ্বারা চিনে থাকে। একইভাবে, গ্রুপের ক্ষেত্রে **Group ID (GID)** ব্যবহার করা হয়। লিনাক্স নাম পড়ার চেয়ে নম্বর দ্রুত প্রসেস করতে পারে, তাই অভ্যন্তরীণভাবে সবকিছু UID এবং GID দিয়ে পরিচালিত হয়।

### UID এবং GID

যদি কোনো ইউজার অ্যাকাউন্ট ডিলিট করা হয় বা তার UID পরিবর্তন করা হয়, তবে ওই ইউজারের তৈরি করা ফাইলগুলোর সাথে আগের নম্বরটি (UID) থেকে যায়। যখন `/etc/passwd` ফাইলে ওই নম্বরের সাথে মেলানো কোনো ইউজার নাম পাওয়া যায় না, তখন লিনাক্স নামের বদলে সরাসরি সেই UID নম্বরটি দেখায়। গ্রুপের (GID) ক্ষেত্রেও একই নিয়ম প্রযোজ্য।

### ইউজার এবং গ্রুপের তথ্য যাচাই করা (`id` কমান্ড)

আপনার বর্তমান ইউজার আইডেন্টিটি, প্রাইমারি গ্রুপ এবং সেকেন্ডারি গ্রুপের সদস্যপদ দেখতে `id` কমান্ড ব্যবহার করা হয়:

```bash
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah),4(adm),27(sudo),1005(research),1006(development)

```

**আউটপুট বিশ্লেষণ:**

* **UID 1001 (`miftah`)**: এটি ইউজারের নিজস্ব আইডেন্টিফায়ার (অ্যাকাউন্ট নম্বর)।
* **GID 1001 (`miftah`)**: এটি প্রাইমারি গ্রুপের আইডেন্টিফায়ার। যখন UID এবং GID এর নম্বর এবং নাম একই হয়, তখন তাকে **User Private Group (UPG)** বলা হয়।
* **Supplemental Groups**: অন্যান্য যেসব গ্রুপের সদস্য এই ইউজার - `adm` (GID 4), `sudo` (GID 27), `research` (GID 1005), এবং `development` (GID 1006)।

### `ls -l` এবং `ls -la` দিয়ে ওনারশিপ যাচাই করা

যখন কোনো নতুন ফাইল তৈরি করা হয়, তখন সেটি অ্যাক্টিভ ইউজার এবং তার প্রাইমারি গ্রুপের অধীনে থাকে:

```bash
miftah@miftahcoding:~$ touch /tmp/filetest1
miftah@miftahcoding:~$ ls -l /tmp/filetest1
-rw-rw-r--. 1 miftah miftah 0 Oct 21 10:18 /tmp/filetest1

```

> **শিক্ষার্থীদের জন্য নোট:** ওপরের আউটপুটে প্রথম `miftah` হলো ইউজারের নাম (User Owner) এবং দ্বিতীয় `miftah` হলো গ্রুপের নাম (Group Owner)।

লুকায়িত বা হিডেন ফাইল (যেসব ফাইলের নাম `.` দিয়ে শুরু হয়) দেখতে `ls -la` ব্যবহার করা হয়। এখানে `.` মানে বর্তমান ডিরেক্টরি এবং `..` মানে প্যারেন্ট (আগের) ডিরেক্টরি:

```bash
miftah@miftahcoding:~$ ls -la
total 60
drwxr-xr-x 1 miftah miftah 4096 Nov  3 22:29 .
drwxr-xr-x 1 root   root   4096 Mar 14  2016 ..
-rw-r--r-- 1 miftah miftah  220 Apr  3  2012 .bash_logout
-rw-r--r-- 1 miftah miftah 3768 Mar 14  2016 .bashrc
drwx------ 2 miftah miftah 4096 Nov  3 22:29 .cache
drwxr-xr-x 2 miftah miftah 4096 Mar 14  2016 Desktop

```

---

## ২. প্রাইমারি গ্রুপ পরিচালনা (`newgrp`, `groups`)

ধরা যাক, আপনি একজন শিক্ষার্থী এবং আপনি 'Science Club' ও 'Debate Club' দুই গ্রুপের সদস্য। ডিফল্টভাবে আপনার প্রাইমারি গ্রুপ 'Science Club'। কিন্তু আপনি যদি চান এখন থেকে তৈরি করা সব ফাইল 'Debate Club' এর নামে সেভ হোক, তবে আপনাকে আপনার প্রাইমারি গ্রুপ পরিবর্তন করতে হবে। লিনাক্সে এই কাজটি `newgrp` দিয়ে করা হয়।

### গ্রুপের সদস্যপদ যাচাই করা

```bash
miftah@miftahcoding:~$ groups
miftah adm sudo research development

```

### অ্যাক্টিভ প্রাইমারি গ্রুপ পরিবর্তন করা

```bash
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah),4(adm),27(sudo),1005(research),1006(development)

miftah@miftahcoding:~$ newgrp research

miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1005(research) groups=1005(research),4(adm),27(sudo),1001(miftah),1006(development)

```

এখন এই নতুন সেশনে আপনি কোনো ফাইল তৈরি করলে সেটি স্বয়ংক্রিয়ভাবে `research` গ্রুপের অধীনে চলে যাবে:

```bash
miftah@miftahcoding:~$ touch /tmp/filetest2
miftah@miftahcoding:~$ ls -l /tmp/filetest2
-rw-r--r--. 1 miftah research 0 Oct 21 10:53 /tmp/filetest2

```

আগের প্রাইমারি গ্রুপে ফিরে যেতে চাইলে সেশন থেকে বের হয়ে যান (`exit` কমান্ডের মাধ্যমে):

```bash
miftah@miftahcoding:~$ exit
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah)...

```

> **নোট:** স্থায়ীভাবে কোনো ইউজারের প্রাইমারি গ্রুপ পরিবর্তন করতে হলে অ্যাডমিনিস্ট্রেটরকে (`root`) নিচের কমান্ডটি চালাতে হবে:
> ```bash
> usermod -g groupname username
> 
> ```
> 
> 

---

## ৩. ওনারশিপ পরিবর্তন (`chgrp`, `chown`)

### গ্রুপের ওনারশিপ পরিবর্তন (`chgrp`)

সাধারণ ইউজাররা তাদের নিজেদের ফাইলের গ্রুপ ওনারশিপ পরিবর্তন করতে পারেন, তবে শর্ত হলো ইউজারকে অবশ্যই কাঙ্ক্ষিত গ্রুপের সদস্য হতে হবে। আর `root` ইউজার যেকোনো ফাইলের গ্রুপ যেকোনো কিছুতে পরিবর্তন করতে পারেন।

```bash
miftah@miftahcoding:~$ touch sample
miftah@miftahcoding:~$ ls -l sample
-rw-rw-r-- 1 miftah miftah 0 Oct 23 22:12 sample

miftah@miftahcoding:~$ chgrp research sample
miftah@miftahcoding:~$ ls -l sample
-rw-rw-r--. 1 miftah research 0 Oct 23 22:12 sample

```

অন্য ইউজারের ফাইলের গ্রুপ পরিবর্তন করতে গেলে আপনি এরর (Error) পাবেন:

```bash
miftah@miftahcoding:~$ chgrp development /etc/passwd
chgrp: changing group of '/etc/passwd': Operation not permitted

```

কোনো ফোল্ডার এবং তার ভেতরের সব ফাইলের গ্রুপ একসাথে পরিবর্তন করতে রিকার্সিভ (Recursive) ফ্ল্যাগ `-R` ব্যবহার করুন:

```bash
miftah@miftahcoding:~$ chgrp -R development test_dir

```

### বিস্তারিত মেটাডেটা দেখা (`stat`)

`stat` কমান্ডটি ফাইলের বিস্তারিত তথ্য দেখায় (যেমন- এক্সেস টাইম, মডিফাই টাইম, UID, GID ইত্যাদি):

```bash
miftah@miftahcoding:~$ stat /tmp/filetest1
  File: `/tmp/filetest1'
  Size: 0         	Blocks: 0          IO Block: 4096   regular empty file
Device: fd00h/64768d	Inode: 31477       Links: 1
Access: (0664/-rw-rw-r--)  Uid: ( 1001/miftah)   Gid: ( 1001/miftah)
Access: 2013-10-21 10:18:02.809118163 -0700
Modify: 2013-10-21 10:18:02.809118163 -0700
Change: 2013-10-21 10:18:02.809118163 -0700

```

### ইউজার ওনারশিপ পরিবর্তন (`chown`)

**ইউজার ওনারশিপ** পরিবর্তন করার জন্য শুধুমাত্র `root` প্রিভিলেজ প্রয়োজন। সাধারণ ইউজাররা চাইলেই তাদের ফাইল অন্য কোনো ইউজারকে দিয়ে দিতে পারেন না (নিরাপত্তা এবং স্টোরেজ কোটা ফাঁকি দেওয়া রোধ করার জন্য)।

#### ব্যবহারের ধরন (Syntax Variations)

১. **শুধুমাত্র ইউজার ওনার পরিবর্তন করা (শুধুমাত্র `root` করতে পারবে):**

```bash
root@miftahcoding:~# chown jane /tmp/filetest1
root@miftahcoding:~# ls -l /tmp/filetest1
-rw-rw-r-- 1 jane miftah 0 Dec 19 18:44 /tmp/filetest1

```

২. **একসাথে ইউজার এবং গ্রুপ দুই-ই পরিবর্তন করা (শুধুমাত্র `root` করতে পারবে):**

```bash
root@miftahcoding:~# chown jane:users /tmp/filetest2

```

৩. **শুধুমাত্র গ্রুপ ওনার পরিবর্তন করা (`:` বা `.` ব্যবহার করে):**

```bash
miftah@miftahcoding:~$ chown .users /tmp/filetest1
miftah@miftahcoding:~$ ls -l /tmp/filetest1
-rw-rw-r-- 1 miftah users 0 Dec 19 18:44 /tmp/filetest1

```

---

## ৪. লিনাক্স পারমিশন বোঝা (Understanding Linux Permissions)

`ls -l` আউটপুটের শুরুতে যে ১০টি ক্যারেক্টার দেখা যায়, সেগুলোই হলো ফাইলের পারমিশন।

```
-rw-r--r--. 1 root root 4135 May 27 21:08 /etc/passwd

```

### ফাইলের ধরন বা File Types (প্রথম ক্যারেক্টার)

| ক্যারেক্টার | ফাইলের ধরন                       | বর্ণনা                           |
| ------ | ------------------------------ | ------------------------------ |
| `-`    | রেগুলার ফাইল (Regular file)        | টেক্সট, ইমেজ বা সাধারণ ডেটা সম্বলিত ফাইল। |
| `d`    | ডিরেক্টরি (Directory)              | ফোল্ডার, যার ভেতরে অন্য ফাইল থাকে।       |
| `l`    | সিম্বলিক লিংক (Symbolic link)       | শর্টকাট ফাইল, যা অন্য ফাইলকে পয়েন্ট করে। |
| `b`    | ব্লক ডিভাইস (Block device)        | হার্ডড্রাইভ বা স্টোরেজ ডিভাইস।           |
| `c`    | ক্যারেক্টার ডিভাইস (Character device) | কিবোর্ড বা মাউসের মতো ডিভাইস।           |

### পারমিশন গ্রুপ (২য় থেকে ১০ম ক্যারেক্টার)

বাকি ৯টি ক্যারেক্টারকে ৩টি করে মোট ৩টি গ্রুপে ভাগ করা হয়:

১. **ইউজার ওনার (User Owner, ক্যারেক্টার ২-৪):** ফাইলের আসল মালিকের পারমিশন।
২. **গ্রুপ ওনার (Group Owner, ক্যারেক্টার ৫-৭):** গ্রুপের অন্যান্য সদস্যদের পারমিশন।
৩. **অন্যান্য (Others, ক্যারেক্টার ৮-১০):** সিস্টেমে থাকা বাকি সবার পারমিশন।

### পারমিশনের ধরন ও এর কাজ

| পারমিশন               | ক্যারেক্টার | ফাইলের ক্ষেত্রে কাজ                  | ডিরেক্টরির (ফোল্ডার) ক্ষেত্রে কাজ                    |
| ------------------- | ------ | ----------------------------- | ---------------------------------------- |
| **পঠন (Read)**      | `r`    | ফাইলের ভেতরের লেখা দেখা বা কপি করা।      | ডিরেক্টরির ভেতরের ফাইলের লিস্ট (`ls`) দেখা।          |
| **লিখন (Write)**     | `w`    | ফাইলের লেখা পরিবর্তন বা সেভ করা।       | ডিরেক্টরির ভেতরে নতুন ফাইল তৈরি করা বা ডিলিট করা।       |
| **সম্পাদন (Execute)** | `x`    | ফাইলটিকে প্রোগ্রাম/স্ক্রিপ্ট হিসেবে রান করানো। | ডিরেক্টরির ভেতরে প্রবেশ করা (`cd` কমান্ড ব্যবহার করে)। |

> **শিক্ষার্থীদের জন্য জরুরি নোট:** কোনো ডিরেক্টরি থেকে ফাইল ডিলিট করার জন্য সেই ডিরেক্টরির `w` (Write) এবং `x` (Execute) পারমিশন থাকা বাধ্যতামূলক। ফাইলের পারমিশনের ওপর ডিলিট হওয়া নির্ভর করে না!

---

## ৫. প্র্যাক্টিক্যাল পারমিশন সিনারিও (Practical Scenarios)

### সেটআপ ডায়াগ্রাম

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr-x. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

---

### সিনারিও ১: ডিরেক্টরি এক্সেসের প্রয়োজনীয়তা

**প্রশ্ন:** নিচের পারমিশন অনুযায়ী, `/data/abc.txt` ফাইলে ইউজার `bob` এর কী কী এক্সেস রয়েছে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr--. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **কোনো এক্সেস নেই।**
* **ব্যাখ্যা:** যদিও `abc.txt` ফাইলে `bob` এর `rwx` (সব) পারমিশন আছে, কিন্তু তার আগের প্যারেন্ট ডিরেক্টরি `/data` তে `others` এর জন্য শুধু `r--` পারমিশন দেওয়া আছে। `x` (Execute) পারমিশন না থাকায় `bob` ওই ফোল্ডারেই ঢুকতে পারবে না।
* **মূল শিক্ষা:** ফাইলের পারমিশনের আগে সবসময় তার প্যারেন্ট ডিরেক্টরির পারমিশন যাচাই করা হয়।

---

### সিনারিও ২: ডিরেক্টরির ভেতরের লিস্ট দেখা

**প্রশ্ন:** এই অবস্থায় কে `ls /data` কমান্ড চালিয়ে ফাইলের লিস্ট দেখতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr--. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **সব ইউজার।**
* **ব্যাখ্যা:** শুধু লিস্ট দেখার জন্য ডিরেক্টরির `r` (Read) পারমিশন প্রয়োজন। `/data` ডিরেক্টরিতে সবার জন্যই `r` পারমিশন রয়েছে। তবে কেউ যদি ফাইলের বিস্তারিত দেখতে চায় (`ls -l /data`), তখন তার `x` পারমিশন লাগবে, যা এখানে শুধু `root` এর আছে।
* **মূল শিক্ষা:** `r` পারমিশন লিস্ট দেখতে সাহায্য করে, কিন্তু ফাইলের ভেতরের মেটাডেটা পড়তে `x` পারমিশন লাগে।

---

### সিনারিও ৩: ফাইল ডিলিট করা

**প্রশ্ন:** কে `/data/abc.txt` ফাইলটি ডিলিট করতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxrw-rw-. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **শুধুমাত্র `root` ইউজার।**
* **ব্যাখ্যা:** ডিরেক্টরি থেকে ফাইল মুছতে হলে ডিরেক্টরির `w` এবং `x` দুটি পারমিশনই লাগে। এখানে সবার `w` পারমিশন থাকলেও, `x` পারমিশন নেই (ফলে তারা ডিরেক্টরিতে ঢুকতে পারবে না)।
* **মূল শিক্ষা:** ফাইল ডিলিট করার ক্ষমতা ফাইলের পারমিশনের ওপর নয়, বরং প্যারেন্ট ডিরেক্টরির পারমিশনের ওপর নির্ভর করে।

---

### সিনারিও ৪: সরাসরি ফাইলের ভেতর পড়া

**প্রশ্ন:** সত্য বা মিথ্যা বলুন: `bob` সফলভাবে `more /data/abc.txt` কমান্ড চালাতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
dr-xr-x--x. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **সত্য।**
* **ব্যাখ্যা:** ফাইল পড়ার জন্য প্যারেন্ট ডিরেক্টরিতে শুধু `x` (Execute) পারমিশন থাকলেই হয়, লিস্ট দেখার জন্য `r` (Read) লাগে না। যেহেতু `bob` জানে ফাইলের নাম কী, তাই সে ডিরেক্টরি পার করে সরাসরি ফাইলে গিয়ে তা পড়তে পারবে।

---

### সিনারিও ৫: ইউজার এবং গ্রুপের জটিলতা

**প্রশ্ন:** `bob` কি `more /data/abc.txt` চালাতে পারবে?

```text
drwxr-xr-x. 17 root    root    4096 23:38 /
dr-xr-x---. 10 sue     payroll  128 03:38 /data
-rwxr-xr--.  1 bob     bob      100 21:08 /data/abc.txt

```

* **উত্তর:** **নির্ভর করছে `bob` এর গ্রুপের ওপর।**
* **ব্যাখ্যা:** যদি `bob` `payroll` গ্রুপের সদস্য হয়, তবে সে ডিরেক্টরির `r-x` পারমিশন পাবে এবং ফাইল পড়তে পারবে। আর যদি সদস্য না হয়, তবে সে `others` (---) হিসেবে গণ্য হবে এবং `Permission denied` এরর খাবে।

---

### সিনারিও ৬: পারমিশনের প্রায়োরিটি (Priority)

**প্রশ্ন:** সত্য বা মিথ্যা বলুন: `bob` কি ফাইলটি পড়তে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
dr-xr-x---. 10 bob  bob  128  03:38 /data
----rw-rwx.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **মিথ্যা।**
* **ব্যাখ্যা:** লিনাক্স সবসময় পারমিশন সিরিয়ালি চেক করে: ইউজার ওনার $\rightarrow$ গ্রুপ ওনার $\rightarrow$ আদার্স। যেহেতু `bob` এই ফাইলের ইউজার ওনার, লিনাক্স শুধুমাত্র তার অংশের পারমিশন (`---`) চেক করবে এবং সেখানেই থেমে যাবে। সে গ্রুপ বা আদার্স পারমিশন চেক করবে না।
* **মূল শিক্ষা:** ফাইলের আসল মালিককে গ্রুপ বা আদার্সের চেয়ে কম পারমিশন দেওয়া বোকামি।

---

## ৬. পারমিশন পরিবর্তন করা (`chmod`)

ফাইল বা ডিরেক্টরির পারমিশন পরিবর্তন করার জন্য `chmod` (change mode) কমান্ড ব্যবহার করা হয়। এটি দুটি পদ্ধতিতে করা যায়: **সিম্বলিক (Symbolic)** এবং **নিউমেরিক বা অক্টাল (Numeric)**।

> **শর্ত:** পারমিশন পরিবর্তন করতে হলে আপনাকে ফাইলের মালিক হতে হবে অথবা `root` প্রিভিলেজ থাকতে হবে।

---

### সিম্বলিক পদ্ধতি (Symbolic Method)

এই পদ্ধতিতে বর্ণ বা সিম্বল ব্যবহার করে পারমিশন নির্ধারণ করা হয়।

#### এর ৩টি অংশ রয়েছে:

১. **কাকে দেবেন (Group)**: `u` (user), `g` (group), `o` (others), `a` (all)
২. **কী করবেন (Operator)**: `+` (যোগ), `-` (বিয়োগ), `=` (সরাসরি নির্ধারণ)
৩. **পারমিশনের ধরন (Type)**: `r` (read), `w` (write), `x` (execute)

#### কিছু উদাহরণ:

**১. গ্রুপের জন্য রাইট (w) পারমিশন যুক্ত করা:**

```bash
miftah@miftahcoding:~$ chmod g+w abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rw-rw-r-- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

**২. একসাথে একাধিক পরিবর্তন করা (কমা ব্যবহার করে):**

```bash
miftah@miftahcoding:~$ chmod ug+x,o-r abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rwxrwx--- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

*(এখানে ইউজার ও গ্রুপের জন্য x যুক্ত করা হয়েছে এবং আদার্সের কাছ থেকে r কেড়ে নেওয়া হয়েছে)*

**৩. `=` ব্যবহার করে নির্দিষ্ট পারমিশন দেওয়া:**

```bash
miftah@miftahcoding:~$ chmod u=rx abc.txt

```

*(এর মানে হলো ইউজারের পারমিশন শুধু r এবং x হবে, যদি w থেকেও থাকে তবে তা মুছে যাবে)*

**৪. সবার জন্য পারমিশন নির্ধারণ করা:**

```bash
miftah@miftahcoding:~$ chmod a+r abc.txt

```

---

### নিউমেরিক (অক্টাল) পদ্ধতি (Numeric Method)

এই পদ্ধতিতে সংখ্যা ব্যবহার করে পারমিশন দেওয়া হয়, যা অনেক দ্রুত এবং প্রফেশনালদের পছন্দের। এখানে প্রতিটি পারমিশনের একটি সাংখ্যিক মান বা ওয়েট (Weight) আছে:

* **Read (`r`)** = 4 (বাইনারি ১০০)
* **Write (`w`)** = 2 (বাইনারি ০১০)
* **Execute (`x`)** = 1 (বাইনারি ০০১)

এই সংখ্যাগুলো যোগ করে যেকোনো পারমিশন তৈরি করা যায়। যেমন, 4+2+1 = 7 মানে হলো `rwx`। 4+0+1 = 5 মানে হলো `r-x`।

#### সংক্ষেপে অক্টাল চার্ট:

| ভ্যালু   | পারমিশন | বর্ণনা                      |
| ----- | ----- | ------------------------- |
| **7** | `rwx` | Read, Write, Execute (সব) |
| **6** | `rw-` | Read, Write               |
| **5** | `r-x` | Read, Execute             |
| **4** | `r--` | শুধু Read                   |
| **3** | `-wx` | Write, Execute            |
| **2** | `-w-` | শুধু Write                  |
| **1** | `--x` | শুধু Execute                |
| **0** | `---` | কোনো পারমিশন নেই               |

#### উদাহরণ (যেমন `754` নির্ধারণ করা):

```bash
miftah@miftahcoding:~$ chmod 754 abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rwxr-xr-- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

*(এখানে 7 মানে ইউজারের rwx, 5 মানে গ্রুপের r-x এবং 4 মানে আদার্সের r--)*

---

## ৭. ডিফল্ট পারমিশন এবং `umask`

যখন লিনাক্সে নতুন কোনো ফাইল বা ফোল্ডার তৈরি হয়, তখন স্বয়ংক্রিয়ভাবে তার একটি পারমিশন সেট হয়ে যায়। এই পারমিশন কত হবে তা নির্ধারণ করে `umask` (user mask)।

### সর্বোচ্চ ডিফল্ট পারমিশন (Max Defaults)

* **ফাইলের ক্ষেত্রে**: `rw-rw-rw-` বা `666` (নিরাপত্তার কারণে ফাইল তৈরির সময় কখনোই স্বয়ংক্রিয়ভাবে `x` বা এক্সেকিউট পারমিশন দেওয়া হয় না)।
* **ডিরেক্টরির ক্ষেত্রে**: `rwxrwxrwx` বা `777`।

নতুন পারমিশন বের করার নিয়ম হলো এই সর্বোচ্চ মান থেকে বর্তমান `umask` বিয়োগ করা।

---

### বর্তমান umask চেক করা

```bash
miftah@miftahcoding:~$ umask
0002

```

**আউটপুট (`0002`) বিশ্লেষণ:**

* **১ম সংখ্যা (`0`)**: এটি বোঝায় যে নম্বরটি অক্টাল ফরম্যাটে আছে।
* **২য় সংখ্যা (`0`)**: ইউজার পারমিশন থেকে কত বাদ যাবে।
* **৩য় সংখ্যা (`0`)**: গ্রুপ পারমিশন থেকে কত বাদ যাবে।
* **৪র্থ সংখ্যা (`2`)**: আদার্স পারমিশন থেকে কত বাদ যাবে।

সাধারণত অ্যাডমিন (`root`) ইউজারদের umask আরও কড়াকড়ি থাকে, যেমন `0022`।

---

### Umask হিসাব করার উদাহরণ

#### উদাহরণ ১: umask যদি `027` হয়

**ফাইলের ক্ষেত্রে হিসাব:**

```text
  666 (ফাইলের সর্বোচ্চ ডিফল্ট: rw-rw-rw-)
- 027 (বর্তমান umask)
------
  640 (তৈরি হওয়া ফাইলের পারমিশন: rw-r-----)

```

প্রমাণ:

```bash
miftah@miftahcoding:~$ umask 027
miftah@miftahcoding:~$ touch sample
miftah@miftahcoding:~$ ls -l sample
-rw-r-----. 1 miftah miftah 0 Oct 28 20:14 sample

```

**ডিরেক্টরির ক্ষেত্রে হিসাব:**

```text
  777 (ডিরেক্টরির সর্বোচ্চ ডিফল্ট: rwxrwxrwx)
- 027 (বর্তমান umask)
------
  750 (তৈরি হওয়া ডিরেক্টরির পারমিশন: rwxr-x---)

```

প্রমাণ:

```bash
miftah@miftahcoding:~$ mkdir test-dir
miftah@miftahcoding:~$ ls -ld test-dir
drwxr-x---. 1 miftah miftah 4096 Oct 28 20:25 test-dir

```

#### উদাহরণ ২: umask যদি `002` হয় (সবচেয়ে সাধারণ)

নতুন ফাইল হবে `666 - 002 = 664` (`rw-rw-r--`)।
নতুন ডিরেক্টরি হবে `777 - 002 = 775` (`rwxrwxr-x`)।

> **পারমানেন্ট সেটআপ:** টার্মিনালে `umask` কমান্ড লিখলে সেটি শুধু ওই সেশনের জন্যই কাজ করে। সেশন কেটে দিলে আবার আগের ডিফল্ট umask চলে আসে। এটিকে স্থায়ী করতে চাইলে আপনার হোম ডিরেক্টরির `~/.bashrc` ফাইলের একদম নিচে `umask 027` (বা আপনার পছন্দের মাস্ক) লিখে সেভ করতে হবে।
> """

file_path_bangla = "linux_permissions_guide_bangla.md"
with open(file_path_bangla, "w", encoding="utf-8") as f:
f.write(bangla_md_content)

print(f"Bangla guide created successfully: {file_path_bangla}")

```

```text?code_stdout&code_event_index=1
Bangla guide created successfully: linux_permissions_guide_bangla.md


```

Here is the complete Bangla version of the guide, expanded with more examples and explanations to make it easy for students to understand. The formatting has been preserved, and the examples continue to use `miftah@miftahcoding`.

### Document Preview

# লিনাক্স ফাইল পারমিশন, ওনারশিপ এবং umask এর পূর্ণাঙ্গ গাইড

এই ডকুমেন্টে লিনাক্সের ফাইল ওনারশিপ (Ownership), গ্রুপ ম্যানেজমেন্ট, পারমিশন (Permissions), পারমিশন অ্যানালাইসিস সিনারিও, পারমিশন পরিবর্তন করার নিয়ম এবং ডিফল্ট umask কনফিগারেশন সম্পর্কে বিস্তারিত আলোচনা করা হয়েছে। শিক্ষার্থীদের সহজে বোঝার জন্য এখানে বাস্তব উদাহরণ এবং ব্যাখ্যা যুক্ত করা হয়েছে।

---

## ১. ফাইল ওনারশিপ (File Ownership) এর ধারণা

সাধারণত, যে ইউজার (User) কোনো ফাইল তৈরি করেন, তিনি সেই ফাইলের মালিক বা 'ওনার' হন। এই ওনারশিপ পরিবর্তন করা সম্ভব, তবে এর জন্য অ্যাডমিনিস্ট্রেটিভ (root) প্রিভিলেজ প্রয়োজন।

যদিও টার্মিনালে কমান্ডগুলো ফাইলের মালিকের নাম টেক্সট হিসেবে দেখায়, কিন্তু অপারেটিং সিস্টেম ভেতরে ভেতরে ইউজারদের একটি নম্বর বা **User ID (UID)** দ্বারা চিনে থাকে। একইভাবে, গ্রুপের ক্ষেত্রে **Group ID (GID)** ব্যবহার করা হয়। লিনাক্স নাম পড়ার চেয়ে নম্বর দ্রুত প্রসেস করতে পারে, তাই অভ্যন্তরীণভাবে সবকিছু UID এবং GID দিয়ে পরিচালিত হয়।

### UID এবং GID

যদি কোনো ইউজার অ্যাকাউন্ট ডিলিট করা হয় বা তার UID পরিবর্তন করা হয়, তবে ওই ইউজারের তৈরি করা ফাইলগুলোর সাথে আগের নম্বরটি (UID) থেকে যায়। যখন `/etc/passwd` ফাইলে ওই নম্বরের সাথে মেলানো কোনো ইউজার নাম পাওয়া যায় না, তখন লিনাক্স নামের বদলে সরাসরি সেই UID নম্বরটি দেখায়। গ্রুপের (GID) ক্ষেত্রেও একই নিয়ম প্রযোজ্য।

### ইউজার এবং গ্রুপের তথ্য যাচাই করা (`id` কমান্ড)

আপনার বর্তমান ইউজার আইডেন্টিটি, প্রাইমারি গ্রুপ এবং সেকেন্ডারি গ্রুপের সদস্যপদ দেখতে `id` কমান্ড ব্যবহার করা হয়:

```bash
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah),4(adm),27(sudo),1005(research),1006(development)

```

**আউটপুট বিশ্লেষণ:**

* **UID 1001 (`miftah`)**: এটি ইউজারের নিজস্ব আইডেন্টিফায়ার (অ্যাকাউন্ট নম্বর)।
* **GID 1001 (`miftah`)**: এটি প্রাইমারি গ্রুপের আইডেন্টিফায়ার। যখন UID এবং GID এর নম্বর এবং নাম একই হয়, তখন তাকে **User Private Group (UPG)** বলা হয়।
* **Supplemental Groups**: অন্যান্য যেসব গ্রুপের সদস্য এই ইউজার - `adm` (GID 4), `sudo` (GID 27), `research` (GID 1005), এবং `development` (GID 1006)।

### `ls -l` এবং `ls -la` দিয়ে ওনারশিপ যাচাই করা

যখন কোনো নতুন ফাইল তৈরি করা হয়, তখন সেটি অ্যাক্টিভ ইউজার এবং তার প্রাইমারি গ্রুপের অধীনে থাকে:

```bash
miftah@miftahcoding:~$ touch /tmp/filetest1
miftah@miftahcoding:~$ ls -l /tmp/filetest1
-rw-rw-r--. 1 miftah miftah 0 Oct 21 10:18 /tmp/filetest1

```

> **শিক্ষার্থীদের জন্য নোট:** ওপরের আউটপুটে প্রথম `miftah` হলো ইউজারের নাম (User Owner) এবং দ্বিতীয় `miftah` হলো গ্রুপের নাম (Group Owner)।

লুকায়িত বা হিডেন ফাইল (যেসব ফাইলের নাম `.` দিয়ে শুরু হয়) দেখতে `ls -la` ব্যবহার করা হয়। এখানে `.` মানে বর্তমান ডিরেক্টরি এবং `..` মানে প্যারেন্ট (আগের) ডিরেক্টরি:

```bash
miftah@miftahcoding:~$ ls -la
total 60
drwxr-xr-x 1 miftah miftah 4096 Nov  3 22:29 .
drwxr-xr-x 1 root   root   4096 Mar 14  2016 ..
-rw-r--r-- 1 miftah miftah  220 Apr  3  2012 .bash_logout
-rw-r--r-- 1 miftah miftah 3768 Mar 14  2016 .bashrc
drwx------ 2 miftah miftah 4096 Nov  3 22:29 .cache
drwxr-xr-x 2 miftah miftah 4096 Mar 14  2016 Desktop

```

---

## ২. প্রাইমারি গ্রুপ পরিচালনা (`newgrp`, `groups`)

ধরা যাক, আপনি একজন শিক্ষার্থী এবং আপনি 'Science Club' ও 'Debate Club' দুই গ্রুপের সদস্য। ডিফল্টভাবে আপনার প্রাইমারি গ্রুপ 'Science Club'। কিন্তু আপনি যদি চান এখন থেকে তৈরি করা সব ফাইল 'Debate Club' এর নামে সেভ হোক, তবে আপনাকে আপনার প্রাইমারি গ্রুপ পরিবর্তন করতে হবে। লিনাক্সে এই কাজটি `newgrp` দিয়ে করা হয়।

### গ্রুপের সদস্যপদ যাচাই করা

```bash
miftah@miftahcoding:~$ groups
miftah adm sudo research development

```

### অ্যাক্টিভ প্রাইমারি গ্রুপ পরিবর্তন করা

```bash
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah),4(adm),27(sudo),1005(research),1006(development)

miftah@miftahcoding:~$ newgrp research

miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1005(research) groups=1005(research),4(adm),27(sudo),1001(miftah),1006(development)

```

এখন এই নতুন সেশনে আপনি কোনো ফাইল তৈরি করলে সেটি স্বয়ংক্রিয়ভাবে `research` গ্রুপের অধীনে চলে যাবে:

```bash
miftah@miftahcoding:~$ touch /tmp/filetest2
miftah@miftahcoding:~$ ls -l /tmp/filetest2
-rw-r--r--. 1 miftah research 0 Oct 21 10:53 /tmp/filetest2

```

আগের প্রাইমারি গ্রুপে ফিরে যেতে চাইলে সেশন থেকে বের হয়ে যান (`exit` কমান্ডের মাধ্যমে):

```bash
miftah@miftahcoding:~$ exit
miftah@miftahcoding:~$ id
uid=1001(miftah) gid=1001(miftah) groups=1001(miftah)...

```

> **নোট:** স্থায়ীভাবে কোনো ইউজারের প্রাইমারি গ্রুপ পরিবর্তন করতে হলে অ্যাডমিনিস্ট্রেটরকে (`root`) নিচের কমান্ডটি চালাতে হবে:
> ```bash
> usermod -g groupname username
> 
> ```
> 
> 

---

## ৩. ওনারশিপ পরিবর্তন (`chgrp`, `chown`)

### গ্রুপের ওনারশিপ পরিবর্তন (`chgrp`)

সাধারণ ইউজাররা তাদের নিজেদের ফাইলের গ্রুপ ওনারশিপ পরিবর্তন করতে পারেন, তবে শর্ত হলো ইউজারকে অবশ্যই কাঙ্ক্ষিত গ্রুপের সদস্য হতে হবে। আর `root` ইউজার যেকোনো ফাইলের গ্রুপ যেকোনো কিছুতে পরিবর্তন করতে পারেন।

```bash
miftah@miftahcoding:~$ touch sample
miftah@miftahcoding:~$ ls -l sample
-rw-rw-r-- 1 miftah miftah 0 Oct 23 22:12 sample

miftah@miftahcoding:~$ chgrp research sample
miftah@miftahcoding:~$ ls -l sample
-rw-rw-r--. 1 miftah research 0 Oct 23 22:12 sample

```

অন্য ইউজারের ফাইলের গ্রুপ পরিবর্তন করতে গেলে আপনি এরর (Error) পাবেন:

```bash
miftah@miftahcoding:~$ chgrp development /etc/passwd
chgrp: changing group of '/etc/passwd': Operation not permitted

```

কোনো ফোল্ডার এবং তার ভেতরের সব ফাইলের গ্রুপ একসাথে পরিবর্তন করতে রিকার্সিভ (Recursive) ফ্ল্যাগ `-R` ব্যবহার করুন:

```bash
miftah@miftahcoding:~$ chgrp -R development test_dir

```

### বিস্তারিত মেটাডেটা দেখা (`stat`)

`stat` কমান্ডটি ফাইলের বিস্তারিত তথ্য দেখায় (যেমন- এক্সেস টাইম, মডিফাই টাইম, UID, GID ইত্যাদি):

```bash
miftah@miftahcoding:~$ stat /tmp/filetest1
  File: `/tmp/filetest1'
  Size: 0         	Blocks: 0          IO Block: 4096   regular empty file
Device: fd00h/64768d	Inode: 31477       Links: 1
Access: (0664/-rw-rw-r--)  Uid: ( 1001/miftah)   Gid: ( 1001/miftah)
Access: 2013-10-21 10:18:02.809118163 -0700
Modify: 2013-10-21 10:18:02.809118163 -0700
Change: 2013-10-21 10:18:02.809118163 -0700

```

### ইউজার ওনারশিপ পরিবর্তন (`chown`)

**ইউজার ওনারশিপ** পরিবর্তন করার জন্য শুধুমাত্র `root` প্রিভিলেজ প্রয়োজন। সাধারণ ইউজাররা চাইলেই তাদের ফাইল অন্য কোনো ইউজারকে দিয়ে দিতে পারেন না (নিরাপত্তা এবং স্টোরেজ কোটা ফাঁকি দেওয়া রোধ করার জন্য)।

#### ব্যবহারের ধরন (Syntax Variations)

১. **শুধুমাত্র ইউজার ওনার পরিবর্তন করা (শুধুমাত্র `root` করতে পারবে):**

```bash
root@miftahcoding:~# chown jane /tmp/filetest1
root@miftahcoding:~# ls -l /tmp/filetest1
-rw-rw-r-- 1 jane miftah 0 Dec 19 18:44 /tmp/filetest1

```

২. **একসাথে ইউজার এবং গ্রুপ দুই-ই পরিবর্তন করা (শুধুমাত্র `root` করতে পারবে):**

```bash
root@miftahcoding:~# chown jane:users /tmp/filetest2

```

৩. **শুধুমাত্র গ্রুপ ওনার পরিবর্তন করা (`:` বা `.` ব্যবহার করে):**

```bash
miftah@miftahcoding:~$ chown .users /tmp/filetest1
miftah@miftahcoding:~$ ls -l /tmp/filetest1
-rw-rw-r-- 1 miftah users 0 Dec 19 18:44 /tmp/filetest1

```

---

## ৪. লিনাক্স পারমিশন বোঝা (Understanding Linux Permissions)

`ls -l` আউটপুটের শুরুতে যে ১০টি ক্যারেক্টার দেখা যায়, সেগুলোই হলো ফাইলের পারমিশন।

```
-rw-r--r--. 1 root root 4135 May 27 21:08 /etc/passwd

```

### ফাইলের ধরন বা File Types (প্রথম ক্যারেক্টার)

| ক্যারেক্টার | ফাইলের ধরন                       | বর্ণনা                           |
| ------ | ------------------------------ | ------------------------------ |
| `-`    | রেগুলার ফাইল (Regular file)        | টেক্সট, ইমেজ বা সাধারণ ডেটা সম্বলিত ফাইল। |
| `d`    | ডিরেক্টরি (Directory)              | ফোল্ডার, যার ভেতরে অন্য ফাইল থাকে।       |
| `l`    | সিম্বলিক লিংক (Symbolic link)       | শর্টকাট ফাইল, যা অন্য ফাইলকে পয়েন্ট করে। |
| `b`    | ব্লক ডিভাইস (Block device)        | হার্ডড্রাইভ বা স্টোরেজ ডিভাইস।           |
| `c`    | ক্যারেক্টার ডিভাইস (Character device) | কিবোর্ড বা মাউসের মতো ডিভাইস।           |

### পারমিশন গ্রুপ (২য় থেকে ১০ম ক্যারেক্টার)

বাকি ৯টি ক্যারেক্টারকে ৩টি করে মোট ৩টি গ্রুপে ভাগ করা হয়:

১. **ইউজার ওনার (User Owner, ক্যারেক্টার ২-৪):** ফাইলের আসল মালিকের পারমিশন।
২. **গ্রুপ ওনার (Group Owner, ক্যারেক্টার ৫-৭):** গ্রুপের অন্যান্য সদস্যদের পারমিশন।
৩. **অন্যান্য (Others, ক্যারেক্টার ৮-১০):** সিস্টেমে থাকা বাকি সবার পারমিশন।

### পারমিশনের ধরন ও এর কাজ

| পারমিশন               | ক্যারেক্টার | ফাইলের ক্ষেত্রে কাজ                  | ডিরেক্টরির (ফোল্ডার) ক্ষেত্রে কাজ                    |
| ------------------- | ------ | ----------------------------- | ---------------------------------------- |
| **পঠন (Read)**      | `r`    | ফাইলের ভেতরের লেখা দেখা বা কপি করা।      | ডিরেক্টরির ভেতরের ফাইলের লিস্ট (`ls`) দেখা।          |
| **লিখন (Write)**     | `w`    | ফাইলের লেখা পরিবর্তন বা সেভ করা।       | ডিরেক্টরির ভেতরে নতুন ফাইল তৈরি করা বা ডিলিট করা।       |
| **সম্পাদন (Execute)** | `x`    | ফাইলটিকে প্রোগ্রাম/স্ক্রিপ্ট হিসেবে রান করানো। | ডিরেক্টরির ভেতরে প্রবেশ করা (`cd` কমান্ড ব্যবহার করে)। |

> **শিক্ষার্থীদের জন্য জরুরি নোট:** কোনো ডিরেক্টরি থেকে ফাইল ডিলিট করার জন্য সেই ডিরেক্টরির `w` (Write) এবং `x` (Execute) পারমিশন থাকা বাধ্যতামূলক। ফাইলের পারমিশনের ওপর ডিলিট হওয়া নির্ভর করে না!

---

## ৫. প্র্যাক্টিক্যাল পারমিশন সিনারিও (Practical Scenarios)

### সেটআপ ডায়াগ্রাম

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr-x. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

---

### সিনারিও ১: ডিরেক্টরি এক্সেসের প্রয়োজনীয়তা

**প্রশ্ন:** নিচের পারমিশন অনুযায়ী, `/data/abc.txt` ফাইলে ইউজার `bob` এর কী কী এক্সেস রয়েছে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr--. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **কোনো এক্সেস নেই।**
* **ব্যাখ্যা:** যদিও `abc.txt` ফাইলে `bob` এর `rwx` (সব) পারমিশন আছে, কিন্তু তার আগের প্যারেন্ট ডিরেক্টরি `/data` তে `others` এর জন্য শুধু `r--` পারমিশন দেওয়া আছে। `x` (Execute) পারমিশন না থাকায় `bob` ওই ফোল্ডারেই ঢুকতে পারবে না।
* **মূল শিক্ষা:** ফাইলের পারমিশনের আগে সবসময় তার প্যারেন্ট ডিরেক্টরির পারমিশন যাচাই করা হয়।

---

### সিনারিও ২: ডিরেক্টরির ভেতরের লিস্ট দেখা

**প্রশ্ন:** এই অবস্থায় কে `ls /data` কমান্ড চালিয়ে ফাইলের লিস্ট দেখতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxr-xr--. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **সব ইউজার।**
* **ব্যাখ্যা:** শুধু লিস্ট দেখার জন্য ডিরেক্টরির `r` (Read) পারমিশন প্রয়োজন। `/data` ডিরেক্টরিতে সবার জন্যই `r` পারমিশন রয়েছে। তবে কেউ যদি ফাইলের বিস্তারিত দেখতে চায় (`ls -l /data`), তখন তার `x` পারমিশন লাগবে, যা এখানে শুধু `root` এর আছে।
* **মূল শিক্ষা:** `r` পারমিশন লিস্ট দেখতে সাহায্য করে, কিন্তু ফাইলের ভেতরের মেটাডেটা পড়তে `x` পারমিশন লাগে।

---

### সিনারিও ৩: ফাইল ডিলিট করা

**প্রশ্ন:** কে `/data/abc.txt` ফাইলটি ডিলিট করতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
drwxrw-rw-. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **শুধুমাত্র `root` ইউজার।**
* **ব্যাখ্যা:** ডিরেক্টরি থেকে ফাইল মুছতে হলে ডিরেক্টরির `w` এবং `x` দুটি পারমিশনই লাগে। এখানে সবার `w` পারমিশন থাকলেও, `x` পারমিশন নেই (ফলে তারা ডিরেক্টরিতে ঢুকতে পারবে না)।
* **মূল শিক্ষা:** ফাইল ডিলিট করার ক্ষমতা ফাইলের পারমিশনের ওপর নয়, বরং প্যারেন্ট ডিরেক্টরির পারমিশনের ওপর নির্ভর করে।

---

### সিনারিও ৪: সরাসরি ফাইলের ভেতর পড়া

**প্রশ্ন:** সত্য বা মিথ্যা বলুন: `bob` সফলভাবে `more /data/abc.txt` কমান্ড চালাতে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
dr-xr-x--x. 10 root root 128  03:38 /data
-rwxr-xr--.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **সত্য।**
* **ব্যাখ্যা:** ফাইল পড়ার জন্য প্যারেন্ট ডিরেক্টরিতে শুধু `x` (Execute) পারমিশন থাকলেই হয়, লিস্ট দেখার জন্য `r` (Read) লাগে না। যেহেতু `bob` জানে ফাইলের নাম কী, তাই সে ডিরেক্টরি পার করে সরাসরি ফাইলে গিয়ে তা পড়তে পারবে।

---

### সিনারিও ৫: ইউজার এবং গ্রুপের জটিলতা

**প্রশ্ন:** `bob` কি `more /data/abc.txt` চালাতে পারবে?

```text
drwxr-xr-x. 17 root    root    4096 23:38 /
dr-xr-x---. 10 sue     payroll  128 03:38 /data
-rwxr-xr--.  1 bob     bob      100 21:08 /data/abc.txt

```

* **উত্তর:** **নির্ভর করছে `bob` এর গ্রুপের ওপর।**
* **ব্যাখ্যা:** যদি `bob` `payroll` গ্রুপের সদস্য হয়, তবে সে ডিরেক্টরির `r-x` পারমিশন পাবে এবং ফাইল পড়তে পারবে। আর যদি সদস্য না হয়, তবে সে `others` (---) হিসেবে গণ্য হবে এবং `Permission denied` এরর খাবে।

---

### সিনারিও ৬: পারমিশনের প্রায়োরিটি (Priority)

**প্রশ্ন:** সত্য বা মিথ্যা বলুন: `bob` কি ফাইলটি পড়তে পারবে?

```text
drwxr-xr-x. 17 root root 4096 23:38 /
dr-xr-x---. 10 bob  bob  128  03:38 /data
----rw-rwx.  1 bob  bob  100  21:08 /data/abc.txt

```

* **উত্তর:** **মিথ্যা।**
* **ব্যাখ্যা:** লিনাক্স সবসময় পারমিশন সিরিয়ালি চেক করে: ইউজার ওনার $\rightarrow$ গ্রুপ ওনার $\rightarrow$ আদার্স। যেহেতু `bob` এই ফাইলের ইউজার ওনার, লিনাক্স শুধুমাত্র তার অংশের পারমিশন (`---`) চেক করবে এবং সেখানেই থেমে যাবে। সে গ্রুপ বা আদার্স পারমিশন চেক করবে না।
* **মূল শিক্ষা:** ফাইলের আসল মালিককে গ্রুপ বা আদার্সের চেয়ে কম পারমিশন দেওয়া বোকামি।

---

## ৬. পারমিশন পরিবর্তন করা (`chmod`)

ফাইল বা ডিরেক্টরির পারমিশন পরিবর্তন করার জন্য `chmod` (change mode) কমান্ড ব্যবহার করা হয়। এটি দুটি পদ্ধতিতে করা যায়: **সিম্বলিক (Symbolic)** এবং **নিউমেরিক বা অক্টাল (Numeric)**।

> **শর্ত:** পারমিশন পরিবর্তন করতে হলে আপনাকে ফাইলের মালিক হতে হবে অথবা `root` প্রিভিলেজ থাকতে হবে।

---

### সিম্বলিক পদ্ধতি (Symbolic Method)

এই পদ্ধতিতে বর্ণ বা সিম্বল ব্যবহার করে পারমিশন নির্ধারণ করা হয়।

#### এর ৩টি অংশ রয়েছে:

১. **কাকে দেবেন (Group)**: `u` (user), `g` (group), `o` (others), `a` (all)
২. **কী করবেন (Operator)**: `+` (যোগ), `-` (বিয়োগ), `=` (সরাসরি নির্ধারণ)
৩. **পারমিশনের ধরন (Type)**: `r` (read), `w` (write), `x` (execute)

#### কিছু উদাহরণ:

**১. গ্রুপের জন্য রাইট (w) পারমিশন যুক্ত করা:**

```bash
miftah@miftahcoding:~$ chmod g+w abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rw-rw-r-- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

**২. একসাথে একাধিক পরিবর্তন করা (কমা ব্যবহার করে):**

```bash
miftah@miftahcoding:~$ chmod ug+x,o-r abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rwxrwx--- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

*(এখানে ইউজার ও গ্রুপের জন্য x যুক্ত করা হয়েছে এবং আদার্সের কাছ থেকে r কেড়ে নেওয়া হয়েছে)*

**৩. `=` ব্যবহার করে নির্দিষ্ট পারমিশন দেওয়া:**

```bash
miftah@miftahcoding:~$ chmod u=rx abc.txt

```

*(এর মানে হলো ইউজারের পারমিশন শুধু r এবং x হবে, যদি w থেকেও থাকে তবে তা মুছে যাবে)*

**৪. সবার জন্য পারমিশন নির্ধারণ করা:**

```bash
miftah@miftahcoding:~$ chmod a+r abc.txt

```

---

### নিউমেরিক (অক্টাল) পদ্ধতি (Numeric Method)

এই পদ্ধতিতে সংখ্যা ব্যবহার করে পারমিশন দেওয়া হয়, যা অনেক দ্রুত এবং প্রফেশনালদের পছন্দের। এখানে প্রতিটি পারমিশনের একটি সাংখ্যিক মান বা ওয়েট (Weight) আছে:

* **Read (`r`)** = 4 (বাইনারি ১০০)
* **Write (`w`)** = 2 (বাইনারি ০১০)
* **Execute (`x`)** = 1 (বাইনারি ০০১)

এই সংখ্যাগুলো যোগ করে যেকোনো পারমিশন তৈরি করা যায়। যেমন, 4+2+1 = 7 মানে হলো `rwx`। 4+0+1 = 5 মানে হলো `r-x`।

#### সংক্ষেপে অক্টাল চার্ট:

| ভ্যালু   | পারমিশন | বর্ণনা                      |
| ----- | ----- | ------------------------- |
| **7** | `rwx` | Read, Write, Execute (সব) |
| **6** | `rw-` | Read, Write               |
| **5** | `r-x` | Read, Execute             |
| **4** | `r--` | শুধু Read                   |
| **3** | `-wx` | Write, Execute            |
| **2** | `-w-` | শুধু Write                  |
| **1** | `--x` | শুধু Execute                |
| **0** | `---` | কোনো পারমিশন নেই               |

#### উদাহরণ (যেমন `754` নির্ধারণ করা):

```bash
miftah@miftahcoding:~$ chmod 754 abc.txt
miftah@miftahcoding:~$ ls -l abc.txt
-rwxr-xr-- 1 miftah miftah 0 Dec 19 18:58 abc.txt

```

*(এখানে 7 মানে ইউজারের rwx, 5 মানে গ্রুপের r-x এবং 4 মানে আদার্সের r--)*

---

## ৭. ডিফল্ট পারমিশন এবং `umask`

যখন লিনাক্সে নতুন কোনো ফাইল বা ফোল্ডার তৈরি হয়, তখন স্বয়ংক্রিয়ভাবে তার একটি পারমিশন সেট হয়ে যায়। এই পারমিশন কত হবে তা নির্ধারণ করে `umask` (user mask)।

### সর্বোচ্চ ডিফল্ট পারমিশন (Max Defaults)

* **ফাইলের ক্ষেত্রে**: `rw-rw-rw-` বা `666` (নিরাপত্তার কারণে ফাইল তৈরির সময় কখনোই স্বয়ংক্রিয়ভাবে `x` বা এক্সেকিউট পারমিশন দেওয়া হয় না)।
* **ডিরেক্টরির ক্ষেত্রে**: `rwxrwxrwx` বা `777`।

নতুন পারমিশন বের করার নিয়ম হলো এই সর্বোচ্চ মান থেকে বর্তমান `umask` বিয়োগ করা।

---

### বর্তমান umask চেক করা

```bash
miftah@miftahcoding:~$ umask
0002

```

**আউটপুট (`0002`) বিশ্লেষণ:**

* **১ম সংখ্যা (`0`)**: এটি বোঝায় যে নম্বরটি অক্টাল ফরম্যাটে আছে।
* **২য় সংখ্যা (`0`)**: ইউজার পারমিশন থেকে কত বাদ যাবে।
* **৩য় সংখ্যা (`0`)**: গ্রুপ পারমিশন থেকে কত বাদ যাবে।
* **৪র্থ সংখ্যা (`2`)**: আদার্স পারমিশন থেকে কত বাদ যাবে।

সাধারণত অ্যাডমিন (`root`) ইউজারদের umask আরও কড়াকড়ি থাকে, যেমন `0022`।

---

### Umask হিসাব করার উদাহরণ

#### উদাহরণ ১: umask যদি `027` হয়

**ফাইলের ক্ষেত্রে হিসাব:**

```text
  666 (ফাইলের সর্বোচ্চ ডিফল্ট: rw-rw-rw-)
- 027 (বর্তমান umask)
------
  640 (তৈরি হওয়া ফাইলের পারমিশন: rw-r-----)

```

প্রমাণ:

```bash
miftah@miftahcoding:~$ umask 027
miftah@miftahcoding:~$ touch sample
miftah@miftahcoding:~$ ls -l sample
-rw-r-----. 1 miftah miftah 0 Oct 28 20:14 sample

```

**ডিরেক্টরির ক্ষেত্রে হিসাব:**

```text
  777 (ডিরেক্টরির সর্বোচ্চ ডিফল্ট: rwxrwxrwx)
- 027 (বর্তমান umask)
------
  750 (তৈরি হওয়া ডিরেক্টরির পারমিশন: rwxr-x---)

```

প্রমাণ:

```bash
miftah@miftahcoding:~$ mkdir test-dir
miftah@miftahcoding:~$ ls -ld test-dir
drwxr-x---. 1 miftah miftah 4096 Oct 28 20:25 test-dir

```

#### উদাহরণ ২: umask যদি `002` হয় (সবচেয়ে সাধারণ)

নতুন ফাইল হবে `666 - 002 = 664` (`rw-rw-r--`)।
নতুন ডিরেক্টরি হবে `777 - 002 = 775` (`rwxrwxr-x`)।

> **পারমানেন্ট সেটআপ:** টার্মিনালে `umask` কমান্ড লিখলে সেটি শুধু ওই সেশনের জন্যই কাজ করে। সেশন কেটে দিলে আবার আগের ডিফল্ট umask চলে আসে। এটিকে স্থায়ী করতে চাইলে আপনার হোম ডিরেক্টরির `~/.bashrc` ফাইলের একদম নিচে `umask 027` (বা আপনার পছন্দের মাস্ক) লিখে সেভ করতে হবে।